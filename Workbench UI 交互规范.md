# Workbench UI 交互规范

生成时间：2026-08-09

本文档规定「此刻便是春天」工作台的用户界面结构与交互行为。新增或修改模块时，应遵循以下规则，避免破坏现有交互一致性。

---

## 左侧标签导航

### 三级层级

左侧标签采用三级结构：`workspace → category → project`。

- **workspace**：顶层工作区，如「自考学习」「阅读资料」。对应右侧切换整个模块内容。
- **category**：中间分组，如自考学习下的「备考科目」「未考科目」。
- **project**：具体项目/科目，如「02324 离散数学」「2026 年高考作文」。

### 视觉区分

不同层级通过缩进和行高区分：

- workspace 顶格，字号最大，行高最宽松
- category 缩进一级，字号中等
- project 缩进两级，字号最小，行高紧凑

同一层级下，相关项目应连续排列，避免跨类别混排。

### 空文件夹占位

`\自考学习\备考科目\` 下的空文件夹应在左侧标签中同步显示为占位项，占位项使用灰色文字或禁用状态，提示用户后续补充内容。

---

## 操作按钮

### workspace 与 category

workspace 和 category 层级的操作通过右侧 `⋯` 按钮触发。点击后展开下拉菜单，包含「重命名」「添加子项」「删除」等操作。

`⋯` 按钮默认半透明，hover 时变为完全可见，避免视觉噪音。

### project

project 层级的编辑/删除图标不长期显示：

- **桌面端**：鼠标 hover 或项目获得焦点时显示
- **移动端**：始终显示，便于触屏操作

编辑图标使用中性色，删除图标使用警示色，两者并排排列。

---

## 右侧内容卡片

### 普通任务卡片

普通任务 project 点击后，右侧显示对应内容卡片，内容直接呈现，不添加额外 tab 切换。

### 自考学习科目卡片

类型为 `core` / `public` / `practice` 的自考科目，右侧卡片顶部提供两个 tab：

- **计划**：显示学习进度、考试安排、复习计划
- **题型分析**：显示历年真题题型分布、高频考点

其他普通自考任务保持单卡片结构，不显示 tab 切换。

---

## 真题页面交互

### 年份区域

每个年份区域顶部提供「原卷」按钮。点击后展开该年份全部页缩略图，再次点击收起。

### 题号链接

每道题目编号旁带有「原卷」文字链接。点击后，右侧页面面板滚动到对应年份的缩略图位置，方便对照原卷。

### 图片展示

题目配图按「一题一图」原则展示，仅显示经人工确认后裁剪的题目相关区域。全页原卷截图不直接嵌入题目区，只能通过「原卷」按钮按需展开。

---

## 高风险操作确认

以下操作必须二次确认：

- 删除文件或文件夹
- 重命名最终交付物
- 跨模块移动文件
- 覆盖已有最终交付物

确认弹窗应清晰说明操作对象和后果，提供「确认」和「取消」两个选项，默认焦点在「取消」上，防止误触。

---

## 响应式适配

### 桌面端

左侧标签栏默认展开显示。窗口宽度足够时，右侧内容区与左侧标签栏同时可见。

### 移动端

左侧标签栏默认收起，通过顶部 `☰` 按钮展开。展开后点击任意 project 自动收起标签栏，留出空间给右侧内容。

主题切换按钮始终显示在顶部右侧，不受侧边栏状态影响。

---

## 修改本规范的时机

出现以下情况时，应同步更新本文档：

- 新增 workspace、category 或 project 层级
- 调整操作按钮的显示/隐藏逻辑
- 新增右侧内容 tab 类型
- 修改真题页面图片展示方式
- 调整响应式布局 breakpoints
