# 项目约束总览

本文档汇总 `E:\TraeWorkToDo` 项目的所有约束规则，并指明每条规则的详细说明位置。新增或修改文件时，先查本文档，再按链接进入具体规范。

## 文件路由约束

文件创建位置由 `zujian-file-router` Skill 决定，核心顺序是：

1. Skill / 基础设施文件 → `.trae/skills/`
2. 最终交付物 → `Workbench/{模块路径}/`
3. 中间产物 → `temp/{模块}/{任务标识}/`

详细规则：`.trae/skills/zujian-file-router/SKILL.md`

关键约定：

- HTML 页面：`{子模块}-{任务}.html`
- JSON 数据：与对应 HTML 同名
- 题目配图：`{year}_q{num}.png`
- 备份目录：`{原名}_备份_YYYYMMDD_HHMMSS`
- 任务临时目录：`YYYYMMDD_{简短描述}`

## Git 与版本控制约束

- 只提交源码、配置、文档和 `Workbench/` 下的非自动生成资料
- 不提交构建产物、临时文件、缓存、旧备份
- 二进制大文件（PDF/Word/图片）走 Git LFS
- 提交信息格式：`<type>: <描述>`

详细规则：`版本控制规范.md`

## 构建与清理约束

- 修改影响 `此刻便是春天.html` 生成的文件后，必须运行 `python build.py`
- 新增或修改模块后，必须运行 `python validate_workbench.py`
- 仅改动静态资料（PDF、Word、图片等）无需调用 `build.py`
- `build.py` 每次构建前后各清理一次临时产物
- 旧备份最多保留 3 份

`validate_workbench.py` 当前会自动检查：

- 嵌入 JS 语法是否合法
- 旧版独立类名（如 `.card`、`.essay`）是否泄漏到工作台
- 阅读模块内容是否完整匹配
- 根目录文件是否在 `文件说明.md` 中有说明
- `Workbench/` 内是否误放了 `.py`、`.ps1`、`.sh`、`.log` 等临时文件
- `.gitignore` 与 `.gitattributes` 是否包含必需规则
- `temp/` 目录是否存在

详细规则：`build.py 使用说明.md`

## 文件说明约束

- 每次新增最终交付物后，必须在 `文件说明.md` 中添加用途说明
- 新增根目录文件或文件夹时，也应在 `文件说明.md` 中说明
- `validate_workbench.py` 会扫描根目录并警告未说明的项

详细规则：`文件说明.md`

## 模块与内容约束

- 工作台名必须为 `此刻便是春天`
- 主 HTML 文件必须位于 `Workbench/此刻便是春天.html`
- 模块数据位于 `Workbench/data/modules/{模块标识}.json`
- 全局配置位于 `Workbench/data/workbench.json`
- 内容类名必须以模块前缀开头，如 `.reading-*`、`.zk-*`
- 禁止通用类名（如 `.card`、`.essay`）直接出现在模块内容中

详细规则：`build.py 使用说明.md`、`文件约束隐患与规避方案.md`

## 资料与图片约束

- 真题配图必须一题一图，且经过人工确认
- 禁止自动生成全页原卷截图作为题目配图
- 禁止关键词触发式自动截图
- 图片区域信息记录在 `image_regions.json`

详细规则：`.trae/skills/zujian-file-router/SKILL.md`

## 文档更新时机

以下情况需要同步更新本文档体系：

- 新增约束类型时 → 更新 `项目约束总览.md`
- 新增检查步骤时 → 更新 `新增文件检查清单.md`
- 新增根目录文档时 → 更新 `文件说明.md`
- 新增 Git 追踪规则时 → 更新 `版本控制规范.md` 和 `.gitattributes`
- 新增文件类型路由时 → 更新 `.trae/skills/zujian-file-router/SKILL.md`
