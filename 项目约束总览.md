# 项目约束总览

本文档是 `E:\TraeWorkToDo` 项目约束规则的**索引入口**，也是新增/修改文件时的**唯一检查清单**。

- 查找具体规范 → 先看「约束索引表」，按链接进入对应权威文档。
- 创建或修改文件 → 直接按「新增/修改文件流程」逐项确认。

除「新增/修改文件流程」外，其他主题的详细规则不再此处展开，避免与权威文档重复。

---

## 约束索引表

| 主题 | 一句话摘要 | 权威文档 | 触发更新时机 |
|---|---|---|---|
| 文件路由与生命周期 | 文件创建位置由 skill 决定，最终交付物进 Workbench，中间产物进 temp | `.trae/skills/zujian-file-router/SKILL.md` | 新增文件类型或变更路由规则时 |
| Git 与版本控制 | 只提交源码/配置/文档和非自动生成资料，二进制大文件走 LFS | `版本控制规范.md` | 新增 Git 追踪规则或变更提交范围时 |
| 构建与清理 | 修改影响 HTML 生成的文件后必须运行 `build.py`，构建前后各清理一次 | `build.py 使用说明.md` | 变更构建流程、模块注册、主题系统或清理规则时 |
| 文件说明 | 新增最终交付物或根目录文件后，必须在 `文件说明.md` 中补充用途说明 | `文件说明.md` | 新增根目录文件/文件夹或最终交付物时 |
| Workbench UI 交互 | 左侧标签三级结构、操作按钮显示逻辑、右侧卡片 tab、真题页面交互、响应式规则 | `Workbench UI 交互规范.md` | 调整 UI 结构、交互或响应式行为时 |
| 模块与内容 | 工作台名固定为「此刻便是春天」，模块数据位于 `data/modules/`，内容类名须带模块前缀 | `build.py 使用说明.md` | 新增模块、变更模块数据格式或类名规范时 |
| 资料与图片 | 真题配图一题一图、人工确认，禁止自动生成全页原卷截图 | `.trae/skills/zujian-file-router/SKILL.md` | 新增资料类型或图片处理规则时 |
| 校验脚本 | `validate_workbench.py` 自动检查 JS 语法、旧类名泄漏、阅读模块完整性、文件说明覆盖、Workbench 结构、Git 配置与 temp 目录 | `.trae/skills/validate_workbench.py` | 新增或调整自动校验项时 |
| 前端改造完整性 | DOM/CSS/JS 三者同步变更；脚本修改 HTML 后必须验证关键选择器与事件处理存在 | `项目约束总览.md` → 「新增/修改文件流程」第二步 | 通过脚本批量修改 HTML 结构或交互时 |
| 前端性能 | 待补充 | `optimize.md` | 引入性能测量或优化策略时 |
| 新增文件检查 | 创建文件前按清单确认位置、命名、生命周期与文档同步 | `项目约束总览.md` → 「新增/修改文件流程」 | 变更文件创建流程或检查项时 |
| 隐患与规避 | 记录尚未自动化的约束风险和后续优化方向 | `文件约束隐患与规避方案.md` | 发现新隐患或落实规避方案时 |
| 代码注释 | Python / JS / SASS 代码必须包含中文注释，说明复杂逻辑、正则、边界处理与外部调用用途 | `项目约束总览.md` → 「新增/修改文件流程」第二步 | 新增或修改代码文件时 |
| 搭建总结 | 记录工作台演进过程、已解决问题与后续建议 | `工作台搭建总结.md` | 架构重大变更或阶段性总结时 |

---

## 常用命令速查

```bash
# 构建工作台
python build.py

# 跳过校验快速构建
python build.py --skip-validate

# 预览清理范围
python build.py --dry-run

# 完整校验
python validate_workbench.py
```

---

## 新增/修改文件流程

每次创建、保存或移动文件时，按以下清单逐项确认。不确定时先保留，再查对应规范。

### 第一步：确定文件位置

- [ ] 这个文件是 Skill / 基础设施文件吗？
  - 是 → 放入 `.trae/skills/` 或对应基础设施位置
  - 否 → 继续下一步

- [ ] 这个文件是最终交付物吗？
  - 是 → 放入 `Workbench/{模块路径}/`
  - 否 → 放入 `temp/{模块}/{任务标识}/`

- [ ] 目标位置已存在同名文件吗？
  - 是，且是最终交付物 → 先备份，再覆盖
  - 是，且是中间产物 → 直接覆盖

### 第二步：命名与代码质量检查

- [ ] HTML 文件名符合 `{子模块}-{任务}.html` 格式
- [ ] JSON 文件名与对应 HTML 同名
- [ ] 文件夹优先使用中文命名，必要时改用拼音
- [ ] 年份格式使用 `YYYY.MM`
- [ ] 新增或修改 Python / JS / SASS 代码时，已为复杂逻辑、正则、边界处理与外部调用添加中文注释
- [ ] 通过脚本批量修改 HTML 结构或交互时，已确认对应的 CSS 选择器与 JS 事件处理同步注入
- [ ] 新增 Tab、折叠、弹窗等交互组件后，已检查 `.active`、显示/隐藏、响应式断点三类样式是否存在

### 第三步：文档同步

- [ ] 新增根目录文件或文件夹时，已在 `文件说明.md` 中添加说明
- [ ] 新增约束类型时，已同步更新 `项目约束总览.md`
- [ ] 新增检查步骤时，已同步更新 `项目约束总览.md` 的「新增/修改文件流程」

### 第四步：Git 相关

- [ ] 不需要提交的文件已确认在 `.gitignore` 中
- [ ] 新增二进制大文件类型时，已同步更新 `.gitattributes`
- [ ] 提交前已运行 `python build.py` 且构建成功
- [ ] 提交前已运行 `python validate_workbench.py` 且校验通过
- [ ] 提交信息符合 `<type>: <描述>` 格式

### 第五步：清理与收尾

- [ ] 本次任务的中间产物已清理，或确认需要保留
- [ ] 没有误把临时脚本、调试输出放到 `Workbench/`
- [ ] 旧备份未超过 3 份

### 常见文件类型快速对照

| 文件类型 | 去向 | 是否提交 Git | 是否 LFS |
|---|---|---|---|
| HTML 最终页面 | `Workbench/{模块路径}/` | 是 | 否 |
| JSON 数据 | 与对应 HTML 同目录 | 是 | 否 |
| PDF/Word 资料 | `Workbench/{模块路径}/` | 是 | 是 |
| 题目配图 | `assets/exam_figures/` | 是 | 是 |
| 处理脚本 | `temp/{模块}/{任务}/` | 否 | 否 |
| OCR 中间文本 | `temp/{模块}/{任务}/` | 否 | 否 |
| 调试输出 | `temp/{模块}/{任务}/调试输出/` | 否 | 否 |
| 构建产物 `此刻便是春天.html` | `Workbench/` | 是 | 否 |

---

## 文档更新原则

- 新增约束类型时 → 先更新本文档索引，再写入对应权威文档
- 新增检查步骤时 → 更新本文档「新增/修改文件流程」
- 新增根目录文档时 → 更新 `文件说明.md`
- 新增 Git 追踪规则时 → 更新 `版本控制规范.md` 和 `.gitattributes`
- 新增文件类型路由时 → 更新 `.trae/skills/zujian-file-router/SKILL.md`
