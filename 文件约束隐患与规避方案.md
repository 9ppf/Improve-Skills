# 文件约束隐患与规避方案

生成时间：2026-08-09

## 一、当前约束体系回顾

目前文件约束分布在以下几个地方：

- `.trae/skills/zujian-file-router/SKILL.md`：文件路由、命名、生命周期管理
- `build.py`：自动清理临时产物、旧备份
- `文件说明.md`：三色清单说明每个文件用途和能否删除
- `项目约束总览.md`：所有约束规则的索引与快速入口
- `build.py 使用说明.md`：构建和清理规则说明
- `版本控制规范.md`：Git 提交范围、LFS、提交信息格式
- `project_memory.md`：项目记忆索引、未成文临时约定、经验教训

这套体系能处理大部分常见问题，但仍有一些隐患点需要提前规避。

## 二、隐患清单与规避方案

### 1. 约束规则分散，记忆成本高

**隐患**：路由规则在 skill 里，清理规则在 build.py 里，文件说明规则在另一个 Markdown 里。新增文件时容易漏掉某一项约束。

**规避**：

- 维护一份统一的「约束总览」文档，列出所有必须遵守的规则和检查时机
- 把 skill、build.py、文件说明中重复的约束统一引用到一处，减少多处维护
- 在每次任务收尾时，按固定检查清单逐项确认

> 当前状态：`项目约束总览.md` 已存在，但过度复述具体规则，需要进一步瘦身为真正的索引文档。

### 2. 文件说明更新依赖人工

**隐患**：虽然 skill 要求新增最终交付物后更新 `文件说明.md`，但实际执行中仍可能忘记，导致文档与实际文件不一致。

**规避**：

- `validate_workbench.py` 已实现 `check_file_documentation()`，扫描根目录文件并警告未说明项
- 后续可扩展为扫描 `Workbench/` 下的新增文件，检查是否在 `文件说明.md` 中有对应条目

> 当前状态：已部分落实，根目录检查已生效。

### 3. Skill 调用缺乏强制校验

**隐患**：创建文件时是否调用了 `zujian-file-router` skill，目前依赖对话中的自觉，没有机制保证每次创建文件都经过路由判断。

**规避**：

- 在 skill 中明确触发条件，并在项目 memory 中强调「任何文件创建必须先调用 skill」
- 后续可以考虑写一个前置检查脚本，扫描新文件位置是否符合规范
- 把 skill 的核心规则提炼成更简短的「文件创建口诀」，降低调用门槛

### 4. 自动清理范围固定

**隐患**：`build.py` 目前只清理已知的几类产物。如果后续使用新的工具或生成新的中间格式，可能会堆积在目录里。

**规避**：

- 在 `build.py` 中增加一个「可疑文件扫描」功能，定期报告根目录和 Workbench 下未在文件说明中登记的文件
- 每次引入新工具时，同步更新 `build.py` 的清理规则
- 建立「中间产物登记册」，记录每种中间产物的来源和清理方式

### 5. temp/ 目录使用不规范

**隐患**：中间产物仍可能保存在根目录或 Workbench 下，尤其是临时脚本、调试输出、OCR 文本等。

**规避**：

- 每次创建临时文件前，先确定任务标识和模块路径，统一放入 `temp/{模块}/{任务标识}/`
- 在 skill 中增加「临时文件创建模板」，自动生成正确的临时目录路径
- 任务结束后立即清理对应临时目录

### 6. 文件命名规范无自动校验

**隐患**：HTML 文件命名应为 `{子模块}-{任务}.html`，但实际可能随意命名，导致后续查找困难。

**规避**：

- 在 `validate_workbench.py` 中增加命名检查
- 检查范围：Workbench 下新增 HTML 文件是否符合命名规范
- 对不符合规范的文件输出警告，要求用户确认或重命名

### 7. 自动清理存在误删风险

**隐患**：`build.py` 会自动删除空目录和旧备份，如果规则写错或用户误配置，可能删除重要文件。

**规避**：

- 清理前增加「保护名单」机制，核心目录如 `.trae/`、`Workbench/`、`temp/`、`transformers/` 永远不被删除
- 删除前打印完整列表，并要求用户确认（可以在 dry-run 之外增加一个 `--confirm` 模式）
- 对最终交付物的删除操作，必须先 Git commit 备份

### 8. 类名前缀约定无通用扫描

**隐患**：`project_memory.md` 要求所有模块类名必须以模块前缀开头，但目前 `validate_workbench.py` 只检查阅读模块的旧类名黑名单，没有覆盖所有通用类名。

**规避**：

- 在 `validate_workbench.py` 中扩展 CSS 类名扫描
- 维护一个通用类名黑名单，如 `.card`、`.btn`、`.list` 等
- 检查生成的 HTML 中是否存在未加模块前缀的通用类名

> 当前状态：已部分落实，阅读模块的 `OLD_CLASSES` 检查已运行。

### 9. 清理目标路径硬编码

**隐患**：`.trae-html-share-packages/` 是 Trae 当前生成的分享包目录，如果未来 Trae 改变路径或命名，清理会失效。

**规避**：

- 用通配符或前缀匹配代替固定路径，如 `.trae-html-share-*`
- 把「Trae 自动生成目录」作为一类抽象，支持后续扩展
- 定期 review 清理规则，确认是否还覆盖当前工具链

### 10. 缺乏文件大小和数量监控

**隐患**：随着资料不断增加，PDF、图片可能占用大量磁盘空间，但没有监控和预警机制。

**规避**：

- 在 `build.py` 或单独脚本中增加目录大小统计
- 对 `Workbench/` 下各模块目录按大小排序，输出 Top10
- 设置阈值警告，如单个模块超过 500MB 时提醒用户整理

### 11. 备份保留策略单一

**隐患**：所有备份统一保留最近 3 份，但不同文件的重要性不同，统一策略可能不够灵活。

**规避**：

- 为核心构建产物（`此刻便是春天.html`）保留 3 份备份
- 为模块输出目录保留 3 份备份
- 对不重要的临时产物不保留备份
- 允许在配置中按文件类型设置保留数量

### 12. 跨模块文件移动缺少规范

**隐患**：如果后续需要把文件在不同模块间移动（例如新增模块后整合历史资料），目前没有明确的操作流程，容易造成文件说明和实际位置不一致。

**规避**：

- 在 skill 中增加「文件移动」流程：先备份、再移动、最后更新文件说明
- 禁止直接手动拖拽文件，要求通过脚本或明确步骤执行
- 移动后重新运行 `build.py` 和 `validate_workbench.py`

### 13. JS 内嵌数据字符串转义遗漏

**隐患**：当大量结构化数据（如背诵卡片、题库等）以 JavaScript 对象字面量内嵌在 `<script>` 标签中时，answer/question 等文本字段内的单引号（如数学补元符号 `a'`、英文缩写 `it's`）若未转义为 `\'`，会导致字符串提前终止，触发 `SyntaxError`，使整个 `<script>` 块执行失败。此类错误属于语法错误，无法被 `try-catch` 捕获，会导致页面所有交互功能静默失效。

**实际案例**：2026-08-14，背诵卡页面 `背诵与简答-核心概念背诵卡.html` 第 597 行离散数学卡片中，补元符号 `a'` 未转义，导致 `SyntaxError: Unexpected token 'else'`，"今日任务"/"总任务"按钮完全失效。bug 存在多个版本未被发现，因为修改后从未在浏览器中实际测试。

**规避**：

- **优先使用外部 JSON 文件存储数据**，JSON 中单引号无需转义，从根本上消除风险
- 若必须内嵌在 JS 中，编写后用 PowerShell 脚本检查每行单引号数量是否为偶数（排除 `\'` 转义）
- 构建流程中加入 `new Function(scriptContent)` 语法预检，在构建阶段拦截语法错误
- **任何功能修改后必须在浏览器中实际测试**（已写入 `项目约束总览.md` 硬性约束）

> 当前状态：已修复转义问题并升级 DATA_VERSION，但自动化检查尚未落实。

## 三、v2.20.x 新增：Skill 化约束建设进度

> 更新时间：2026-08-22
> 背景：v2.20.0 前后的知识框架设计稿还原任务中，暴露出大量"软约束靠自觉、自觉靠不住"的问题。启动 Skill 化约束建设，将软性约束转为硬性约束。

### 3.1 建设目标

将分散在各文档中的软约束，通过「Skill 文档 + 校验脚本」的方式固化，形成可执行、可检查的工作流程。

### 3.2 已完成（3个 Skill）

| # | Skill 名称 | 核心约束 | 配套校验 | 校验力度 | 版本 |
|---|---|---|---|---|---|
| 1 | `structural-change-workflow` 结构性变更 | 先讨论再编码、不修改现有面板、方案对比代替开放式提问、完成标准可验证、实用优先 | `check_structural_change.py`（新增页面/模块配置/导航变更检测） | advisory | v2.20.0 |
| 2 | `documentation-versioning` 文档与版本管理 | 7份文档分工同步、提交前必须验收、版本号三同步、破坏性操作界定、提交信息 `[验收通过]` 标记 | 版本号同步 + CHANGELOG覆盖 + 验收标记 + 文件说明覆盖（`check_file_doc_coverage.py`） | 混合 | v2.20.0 |
| 3 | `ui-design-restoration` 设计稿1:1精确还原 | 三档位原则（1:1精确/大致对齐/创作）、默认大致对齐档、1:1精确档才列差异清单、CSS 9条专项约束（通用）、全局样式先问后改、知识点优先级+颜色区分、缓存控制、2次不对主动求助、越界界定 | `check_css_standards.py`（!important/裸类名/全局标签/硬编码色值/全局变量）+ `check_priority_labels.py`（三科知识点标签+颜色） | advisory | v2.20.0 |

### 3.3 未完成（2个 Skill）

#### Skill 4：验证与交付（verification-delivery）

**核心痛点**：
- 绕过工作台验证（直接打开HTML，不是用户实际看到的）
- 说"已验证"但无证据
- 修一个问题不查相邻问题（挤牙膏式修复）
- 迁移类任务不逐个验证
- "完成"定义不一致

**约束项（共7项）：**

| 排序 | 约束内容 | 能否自动化 | 现状 |
|---|---|---|---|
| 1 | 实现/修改后必须在浏览器中实际测试 | ❌ 行为层面，无法校验 | 软约束，靠自觉 |
| 2 | 验证必须通过工作台完整路径（首页→侧边栏→iframe） | ❌ 行为层面，无法校验 | 软约束，靠自觉 |
| 3 | 修完必须主动查相邻问题，交付前交叉检查 | ❌ 行为层面，无法校验 | 软约束，靠自觉 |
| 4 | 宣布"完成"前对照验收标准逐项确认 | ⚠️ 部分可校验 | 软约束 |
| 5 | 每次变更后给"改动清单" | ⚠️ git diff 可自动生成 | 部分已有 |
| 6 | 迁移类任务必须逐个目标页面验证 | ❌ 行为层面，无法校验 | 软约束 |
| 7 | 验证输出必须包含截图 + computed style 证据 | ❌ 行为层面，无法校验 | 软约束 |

**待决策**：是否做独立 Skill？行为层面约束怎么保证？

#### Skill 5：代码与文件质量（code-file-quality）

**核心痛点**：文件乱放、类名冲突、代码没注释、JS转义错误

**现状**：大部分校验已分散在各脚本中，缺统一 Skill 文档

| 约束 | 校验状态 |
|---|---|
| 文件路由规范（Workbench vs temp） | ✅ `check_temp_cleanliness.py` 已有 |
| 类名必须带模块前缀 | ✅ `check_css_standards.py` 裸类名检查已有 |
| 代码必须包含中文注释 | ✅ validate_workbench.py 已有 |
| JS 内嵌数据引号转义 | ⚠️ JS 语法检查已有，转义专项待补 |
| Python 代码必须自行校验 | ❌ 无法校验 |
| HTML 文件命名规范 | ✅ validate_workbench.py 已有 |
| 文件夹中文命名 | ✅ validate_workbench.py 已有 |
| 文件说明.md 覆盖 | ✅ `check_file_doc_coverage.py` 已有 |

**待决策**：要不要单独做 Skill？还是合并到现有 Skill？

### 3.4 难以自动化的约束（行为层面）

以下约束属于"我做没做只有我知道"的类型，脚本无法检测，是当前约束体系的最大盲区：

| 约束 | 所属 Skill | 为什么难自动化 |
|---|---|---|
| 必须走工作台完整路径验证 | 验证与交付 | 脚本不知道我是怎么打开浏览器的 |
| 必须逐项检查相邻问题 | 验证与交付 | 脚本不知道我查了没查 |
| 必须对照验收标准逐项确认 | 验证与交付 | 脚本不知道我是扫一眼还是逐项对 |
| 连续2次不对必须主动求助 | 设计稿还原 | 脚本不知道我是不是在闷头试 |
| Python 代码必须自行校验逻辑 | 代码与文件质量 | 脚本不知道我有没有自测 |

**待讨论解决方案**：
- 方案A：交付检查清单模板（每次交付前我必须填，你对照检查）
- 方案B：纯流程规定，发现违规就重罚（但怎么发现是个问题）
- 方案C：你有更好的想法

### 3.5 建议执行顺序

1. **先确认方向**：未完成的2个 Skill 做不做？怎么做？
2. **行为层约束**：讨论出可行的保障机制
3. **补全文档**：确认后一次性写入对应 Skill 和校验脚本

---

## 四、v2.20.x 之前的隐患与规避方案（历史）

> 以下为 2026-08-09 初始版本内容，部分已在 v2.20.0 前后通过 Skill 化建设落实。

### 1. 约束规则分散，记忆成本高

#### 立即执行

1. 把「更新文件说明」检查项加入 `validate_workbench.py`（根目录扫描已完成，可扩展至 Workbench 子目录）
2. 在 `build.py` 清理逻辑中增加核心目录保护名单
3. 优化 `项目约束总览.md`，使其回归索引职责

#### 近期执行

1. 增加类名前缀通用扫描
2. 增加文件命名规范检查
3. 完善清理规则的保护名单和确认机制
4. 把硬编码清理路径改为通配或配置化

#### 中期执行

1. 增加目录大小监控
2. 优化备份保留策略
3. 完善跨模块文件移动规范
4. 建立「中间产物登记册」

## 五、核心原则

- **宁可多一步确认，不要一次误删**
- **任何新约束都要同步到 skill、校验脚本和文件说明三个地方**
- **自动化能解决的问题不要依赖记忆**
- **临时产物和最终交付物必须物理隔离**
