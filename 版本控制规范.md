# 版本控制规范

本文件规定 `E:\TraeWorkToDo` 仓库的提交范围、提交时机与提交信息格式。所有涉及 Git 的操作以本文为准。

## 仓库基本信息

- 仓库位置：`E:\TraeWorkToDo`
- 仓库类型：本地仓库，不强制要求远程
- Git 可执行文件：`E:\Git\Git\cmd\git.exe`

## 提交范围

### 必须提交的文件

这些文件是工作台的源码、配置和文档，缺失后无法重新生成或理解工作台：

- `build.py` — 构建脚本
- `transformers/` — 构建转换器
- `.trae/skills/` — Skill / 基础设施脚本
- `Workbench/data/` — 全局配置与模块数据
- `Workbench/read/` — 阅读模块原始 HTML 资料
- `Workbench/自考学习/` 下的非自动生成资料（PDF、Word、HTML 输出、JSON 等）
- 根目录文档：`文件说明.md`、`build.py 使用说明.md`、`工作台搭建总结.md`、`文件约束隐患与规避方案.md`、`版本控制规范.md`
- `.gitignore` — Git 忽略规则

### 禁止提交的文件

以下文件由工具自动生成或属于临时产物，不应进入版本控制：

- `Workbench/此刻便是春天.html` — 由 `build.py` 生成的构建产物
- `temp/` — 临时工作目录
- `__pycache__/` — Python 缓存
- `.trae-html-share-packages/` — Trae 自动生成的分享包
- `Workbench/此刻便是春天.html.bak-*` — 工作台 HTML 旧备份
- `Workbench/自考学习/备考科目/02324离散数学/真题输出_备份_*/` — 真题输出旧备份
- `~$*` — Word 临时文件
- `.DS_Store`、`Thumbs.db` — 系统文件

## 提交时机

在以下情况下执行提交：

- 新增或修改影响 `此刻便是春天.html` 生成的文件后（如 `workbench.json`、模块 JSON、转换器、阅读资料 HTML）
- 新增模块并注册后
- 修改构建脚本、校验脚本、Skill 基础设施后
- 更新项目文档后

以下情况通常不需要单独提交：

- 仅改动静态资料（PDF、Word、图片等），除非这些资料是首次加入
- 运行 `build.py` 后未修改任何源码，仅重新生成了 `此刻便是春天.html`

## 提交前检查

执行提交前，按以下顺序确认：

1. 运行 `python build.py`，确保构建成功
2. 运行 `python validate_workbench.py`，确保校验通过
3. 检查 `git status`，确认没有误把临时产物或构建产物加入暂存区
4. 确认新增文件已在 `文件说明.md` 中补充说明

## 提交信息格式

提交信息采用以下格式：

```
<type>: <简短描述>

<可选的详细说明>
```

### 类型说明

- `init` — 初始化仓库或首次提交
- `feat` — 新增功能或模块
- `fix` — 修复问题
- `refactor` — 重构源码，不改变外部行为
- `docs` — 仅修改文档
- `chore` — 构建流程、工具配置等杂项
- `theme` — 主题相关修改

### 示例

```
init: 此刻便是春天工作台源码

包含 build.py、模块数据、转换器、Skill 脚本与项目文档。
```

```
feat: 新增阅读模块 2026 年高考作文
```

```
fix: 修复 build.py 重复注入主题 CSS 的问题
```

## 与 build.py 的关系

- `build.py` 负责生成构建产物，但不自动提交
- 提交是独立动作，由人工或明确指令触发
- 提交前应先通过 `build.py` 和 `validate_workbench.py` 验证当前源码状态

## 首次提交

首次提交应包含所有必须提交的源码和文档，提交信息使用：

```
init: 此刻便是春天工作台源码
```
