# 用户偏好档案与 AI 协作建议（合并版）

> 生成时间：2026-08-15  
> 最后更新：2026-08-23（合并用户偏好与 AGENT_HANDOFF 中的用户偏好内容）  
> 用途：新 Agent 必读。先读「性格速写」找感觉，再读后面的细节。  
> 来源：用户偏好与AI协作建议.md + AGENT_HANDOFF_reorganized.md 中的用户偏好部分

---

## 一、用户背景与画像

### 1.1 详细画像

| 维度 | 内容 |
|---|---|
| 身份 | 自考本科生（武汉理工大学·计算机科学与技术），前端开发背景，目前在上海嘉定 |
| 考试目标 | 2026年10月自考（计算机系统原理、离散数学、数据结构与算法三科） |
| 职业目标 | 考后立即转型 AI 岗位，需在学习阶段同步准备岗位技能和简历级项目 |
| 技术水平 | 前端熟练；Python 零基础；AI 初学者 |
| GitHub | 有账号 |
| 时间分配 | 自考 70%、AI 学习 10%、Python 基础 10%、休息 10% |
| 学习周期 | 10 周冲刺备考 → 考后 12 周岗位转型 |
| 备考工具 | 飞书文档（笔记）、本工作台（学习引导+进度追踪） |

### 1.2 工作台角色定位

工作台扮演三个角色：
- **教练**：制定计划、分配任务、追踪进度、提醒复盘（学习驾驶舱、今日学习流、番茄钟）
- **家教**：讲解知识点、出题测验、答疑解惑（知识框架页、背诵卡测验、AI答疑）
- **项目经理**：管理岗位技能图谱、简历项目追踪、学习闭环（岗位技能图谱、岗位学习闭环）

---

## 二、用户性格速写

> 几句话快速建立对用户的整体印象，比读完整篇文档先有个"人"的概念。

1. **讨厌自作主张** — 说改A就只改A，"顺手优化"=越界=炸。指令边界感极强。
2. **细节控** — 颜色差一点、圆角差2px、间距不对，一眼就能看出来。对"粗糙"零容忍。
3. **容易在工具上花时间** — 看到不顺手/看不懂的地方就想调整，改着改着容易偏离主线。部分原因是英文文件名和模块名需要反应时间，增加了认知负担。需要有人帮忙拉回主线。
4. **追求控制感** — 怕漏掉重点、怕数据丢失、怕不知道进度。一切要清晰、可见、自己说了算。
5. **审美偏"干净精致"** — 简洁但不简陋，有细节但不花哨。乱、杂、堆砌 = 难受。
6. **直接型沟通** — 不喜欢绕弯子，有话直说。生气了也直接说，不憋。

---

## 三、核心焦虑与情绪管理

### 3.1 核心焦虑

- **"怕漏掉关键内容"** — 用户反复表达的核心焦虑。知识点必须标注优先级和学习方式（见五-5.3内容设计偏好）。
- **怕数据丢失** — 掌握状态、AI对话历史、题库等关键数据必须持久化，换浏览器、换 Agent 都不能丢。
- **怕不知道进度** — 一切要清晰、可见、自己说了算。

### 3.2 焦虑驱动的偏好

- 改知识框架时必须检查是否有章节被遗漏
- 校验时增加覆盖率检查
- 掌握状态/AI对话/题库通过 API 持久化到 JSON 文件

---

## 四、用户雷区与协作规范

> 合并了原红线、沟通姿势、忠告三部分，按愤怒程度排列。选择最合适的描述，不重复。

### 4.1 用户雷区（按愤怒程度排序）

**🔴 最炸（碰了必炸，没有之一）**

1. **不要自作主张** — 说改A就只改A，没提的一律不动。"顺手优化一下" = 越界 = 用户暴怒。"严格"是指指令边界：你说改什么就改什么，没说的一律不动。但"精确"是分档位的：默认"大致对齐"，你明确说"1:1精确"才按像素级还原。
2. **UI 问题先看再修，不许猜** — 用户说"显示不对"，先打开页面看 DOM、看可见元素数量、对比正常页面，**不许猜**。"应该是缓存"=找死。"我代码里看着没问题"=没查。必须亲眼确认现象 → 定位根因 → 修复 → 验证，四步缺一不可。
3. **"多了一个"=结构溢出**（特殊标记）— 用户反馈"多了一个按钮/元素"时，优先检查是否有元素从 pane 里跑出来了（div 闭合错误）。查可见元素数量（不是总数量），查元素的最近 pane 父节点。用 `element.closest('.chapter-tab-pane')` 或检查 offsetTop 判断元素是否在正确的 pane 内。

**🟠 很炸**

4. **不要绕过验证** — 必须通过工作台（侧边栏→iframe）验证，直接打开HTML = 没验证。侧边栏→点对应模块→看iframe效果。直接打开HTML = 自欺欺人。
5. **不要闷头试错** — 同一个问题改2次还不对，立即停下求助，说明卡在哪了。别反复试错浪费用户时间，停下来说明你理解的需求、做了什么、卡在哪。
6. **交付必须问"可以提交吗？"** — 不能只说"做完了"，必须明确征求验收同意，等用户确认后再提交Git。

**🟡 一般**

7. **用"应该不影响""我觉得没问题"代替验证** — 没验证 = 没完成。
8. **问开放式问题让用户做决策** — 别问"你觉得怎么做？"，要问"A方案XX，B方案YY，推荐A，你选哪个？"。
9. **出错只说"修好了"不给根因** — 别只说"修好了"，说清为什么会发生、怎么防止再发生。用户偏好系统性解决方案，先解释根因再给修复方案，不要只修表面症状。
10. **默认"大致对齐档"** — 有设计稿/有参照的任务，默认先做一版让用户挑问题，别上来就像素级抠。用户明确说"1:1精确"再升级。1:1档才需要先列差异清单确认，其他档位直接做。
11. **响应简短直接** — 一句话说清状态，技术细节放代码和文档里。
12. **出错主动说清楚** — 别只说"我做不好"，要说清：我理解的需求→我做了什么→结果是什么→我卡在哪。
13. **每次文档变更后给"改动清单"** — 不要只说"已更新文档"。必须列出：文件名、章节名、行号范围、具体改了什么（新增/修改/删除）。用户打开文件时能直接定位到改动，不用自己找。
14. **宣布"完成"前对照验收标准** — 方案应包含可验证的"完成标准"。如果用户没给，主动提一句"完成标准：XXX，你看对不对？"。功能能跑 ≠ 任务完成。
15. **先读文档再动手** — `用户偏好与AI协作建议_合并版.md`（本文档）→ `AGENT_HANDOFF_reorganized.md` → `项目约束总览.md`，按顺序读。
16. **版本号同步更新** — 改了内容页必须改`self-study.json`里的`?v=`，否则用户看不到更新。
17. **结构性变更先讨论** — 增删模块/改导航/改信息架构，先出方案再动手。
18. **区分提问与指令** — 用户问"为什么/还有没有/是什么"时，只回答原因，不动手改；用户说"把XX改了/记录到文档"时才执行。不要用行动代替回答。
19. **打开工作台用内置浏览器 + 启动本地服务器** — 用户说"打开工作台"时，必须先启动 `python dev_server.py` 本地服务器，再用内置浏览器打开 `http://localhost:8000/Workbench/此刻便是春天.html`，确保能看到完整数据（API 调用、持久化数据等），不能直接双击 HTML 文件。

> 记住：用户要的是听话+靠谱的助手，不是"有想法"的设计师。

### 4.2 工作模式三档位

不是简单的"还原 vs 创作"，而是三档。默认是第二档（大致对齐），用户明确说才升档或降档。

| 档位 | 触发条件 | 做法 | 精度 | 积分消耗 |
|---|---|---|---|---|
| **🔴 1:1精确档** | 用户说"1:1还原""一模一样""逐像素对齐" | 先列差异清单→用户确认→严格执行 | 每个细节都对 | 最高（5-8轮+） |
| **🟡 大致对齐档（默认）** | 用户说"按设计稿来""大概做成这样""差不多这个意思" | 先做一版→用户挑问题→一轮改完 | 整体感觉对，关键细节到位 | 最少（2-3轮） |
| **🟢 创作档** | 用户说"做一个XX""你看着办""好看一点" | 主动出2-3个方案→用户选→执行 | 自由发挥 | 中等（方案讨论为主） |

**判断方法**：
- 有没有明确目标或参照？没有 → 创作档
- 有参照，但用户有没有说"1:1""精确""一模一样"？说了 → 1:1精确档，没说 → 大致对齐档
- **默认按大致对齐档执行**，除非用户明确升级

---

## 五、设计与视觉偏好

### 5.1 布局与交互

- **工作台布局**：左侧分层标签（主分类 + 子项），右侧对应计划内容
- **编辑/删除按钮**：桌面端 hover/focus 时显示，移动端永久显示。避免界面杂乱。（侧边栏添加按钮已隐藏，因为当前不需要手动添加）
- **内容切换方式**：偏好按钮切换（同 AI 规划师按钮风格），而非 tab 切换。知识框架页的「学习计划 / 知识总览」就是按钮切换。
- **AI 对话入口**：要求直接可见，不藏在菜单里
- **对话历史**：只保留最近 20 条，超出自动截断，避免 localStorage 膨胀
- **对话消息折叠**：默认显示 2-3 行，超出用渐变遮罩收起，底部"展开 ▼"按钮

### 5.2 视觉风格

- **按钮配色**：只有"添加"类按钮保留渐变色，编辑/删除等操作按钮用纯色
- **图标风格**：中国风可爱 emoji（🏮📚🎋🍑🍵🌸🎐），且要求可修改
- **主色调**：蓝色系（主色 `#3b82f6`），section 标题加左竖条，标题左对齐 + emoji 前缀
- **页面风格**：简洁，去掉径向渐变背景和毛玻璃效果
- **重点内容必须醒目** — 知识点优先级不能只用标签，必须用背景色区分（重点=红色背景、一般=黄色背景、了解=灰色背景），配合图标和排序，让用户一眼抓住重点。背景色区分 > 字体颜色区分 > 纯标签。
- **知识点优先级必须颜色区分** — 不能只用文字标签（🔴重点/🟡一般/🟢了解），必须用字体颜色或背景色区分。

### 5.3 内容设计偏好

- **知识点必须有优先级和学习方式标注** — 每个知识点标注优先级（重点/一般/了解）和学习方式（计算/背诵/理解/应用），减少"该重点学的内容被遗漏"的焦虑。
- **确认范围** — 明确哪些改、哪些不改。用户说"核心概念不动"就绝对不动。
- **不修改已完成框架的现有面板** — 用户偏好保持已有框架的完整性，新增功能尽量以独立面板/模块方式添加，而非改动现有面板。
- **数据版本控制偏好** — 修改方案前先做版本记录并提交 Git，方便后续回溯。
- **最小化过度工程** — 用户对细节敏感，偏好简洁直接的方案，反对不必要的抽象和过度设计。
- **数据来源与一致性偏好**：
  - **数据统一管理** — 背诵卡数据不要独立JSON文件，直接从知识框架JSON读取，减少维护多个数据源导致的不一致。知识框架是唯一真相源，各页面（背诵卡/练习测验/复盘）都从这里取数。
  - **版本号统一来源** — 页面数据缓存版本从URL的 `v` 参数读取（和 `self-study.json` 里的版本号同步），不要维护多个独立版本号，容易忘同步导致缓存不刷新。
  - **命名三处一致硬约束** — 知识框架章节名 = 题库chapter字段 = 题库cardId与知识框架term一致，命名对不上闭环就断链。
  - **静态HTML只做兜底，最终目标是纯动态渲染** — 数据从JSON加载，页面不写死内容，避免数据更新了页面还显示旧的。
  - **教材页码以教材正文页码为准**，不是PDF页码（PDF包含封面/大纲等前面几页）。

---

## 六、学习规划偏好

- **时间分配**：70% 自考、10% Python 基础、10% AI 学习、10% 休息
- **学习阶段**：初级阶段（第1-5周）→ 中间阶段（第6-7周）→ 冲刺阶段（第8-10周）
- **每日时段**：08:00-11:30 上午主科 / 14:00-16:00 下午主科 / 16:30-17:30 Python / 19:30-20:30 AI / 20:30-21:30 英语 / 22:00-23:00 整理复盘
- **学习闭环**：见八-产品愿景#3（详细偏好见下方 6.1）
- **AI 直接生成计划**：见八-产品愿景#4
- **数据不丢失**：见三-3.1核心焦虑

### 6.1 学习闭环偏好

- **一章一章推进，不一下全改** — 先把一章的闭环跑通做样板，确认没问题再扩展到后面章节。反对"一次性全部更新"，觉得那样浪费时间在工具打磨上。
- **完整闭环 = 知识框架 + 背诵卡 + 练习测验 + 复盘总结**，四环缺一不可。但补充知识点时**只改两个JSON（知识框架 + 题库）**，页面代码不动，数据自动生效，避免改代码引入新bug。
- **题量标准：每个知识点4-5题**，覆盖选择/填空/简答/计算/证明等题型，题量太少达不到反复练习的目的。
- **练习测验三层筛选**：知识点（蓝紫色）+ 题型（紫色）+ 掌握程度（青绿色），用下拉框不用一堆按钮，要简洁。
- **背诵卡分记忆卡+计算卡**：记忆卡是文字表述定义，计算卡是符号表达式/公式，两类卡片都从知识框架JSON读取。
- **复盘总结要能按状态筛题库**：未答/答对/答错，方便批量复习错题。
- **新增知识点用标准提示词**，约束"只改两个JSON不动代码"，有专门的提示词模板文件（`新增知识点提示词模板.md`）。
- **知识点 level 按最高要求层级标注** — 同时包含识记和领会的知识点，level 标为"领会"（因为识记是领会的基础，能领会就意味着也要识记）。

---

## 七、工作习惯

- **先讨论再动手**：生成任何文件前必须先确认方案，确保产出符合预期（结构 vs 内容的详细区分见四-4.1雷区#17）
- **实现即测试**：功能实现后必须在浏览器中实际测试，不能仅凭代码推断
- **代码注释**：Python / JS / SASS 代码必须包含中文注释
- **小步提交**：每完成一个小功能就提交一次，别攒一堆才commit
- **不主动做关联检查是最大的浪费** — 修了一个问题后必须主动查相邻问题。用户曾多次连续追问"还有什么问题"，每轮都发现新问题——说明第一次就应该做完整审计，而非逐轮挤牙膏。规则：任何文档类任务，交付前至少跑一遍交叉检查（版本号一致性、引用时效性、相邻章节是否过时）。
- **文档一致性是硬约束不是软建议** — CHANGELOG.md、AGENT_HANDOFF.md、工作台搭建总结.md 三份文档的版本号必须同步，后续建议必须反映最新状态，FAQ 必须覆盖已知问题。

---

## 八、产品愿景偏好

> 用户期望的工作台是什么样子——这些偏好指导功能设计方向。

1. **主动引导，而非被动展示** — 工作台应该告诉用户"今天学什么、怎么学、学多久"，而不是让用户自己去找内容。AI 统筹规划师、今日学习流、番茄钟任务闹钟都是这一理念的体现。
2. **重点醒目，减轻焦虑** — 知识点必须用背景色、图标等视觉手段让用户一眼抓住重点，消除"遗漏关键内容"的焦虑（优先级标注规则见五-5.3）。
3. **学习闭环** — 知识框架 → 背诵卡片 → 测验题库 → 真题练习 → 复盘总结，每个环节的数据要打通，掌握状态联动更新。
4. **AI 直接生成计划** — 用户期望 AI 能直接调用工具生成学习计划，而不是只给提示词让用户自己去问。AI 统筹规划师（对话式生成每日计划）和岗位技能图谱的教学指引（tutorial+practice+completion）都是这一理念的体现。
5. **数据不丢失** — 见三-3.1核心焦虑。

---

## 九、自主度边界

### 9.1 可以直接做的
往已有结构填数据、修复 bug、调整样式、更新文档、运行构建校验。

### 9.2 必须先确认的
增删模块/分类、重组导航、改变信息架构、删除文件或分支、Git push、修改约束规范。

### 9.3 绝不做的
`git reset --hard`、`git checkout .`、`git clean -f`、硬编码 API Key、跳过 pre-commit hook（除非用户明确要求）。

---

## 十、技术盲区

- **Python 代码用户无法审查** — 用户 Python 零基础，生成 Python 代码后必须自行校验逻辑合理性，不能依赖用户发现错误。涉及 Python 的变更需额外说明做了什么、为什么。
- **浏览器缓存问题是高频痛点** — 用户多次因缓存导致看不到更新而困惑。任何涉及前端页面的变更，必须同时给出缓存控制方案（版本参数 + HTTP 头），不能只改内容不管缓存。
