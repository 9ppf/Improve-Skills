# 用户偏好档案与 AI 协作建议

> 生成时间：2026-08-15  
> 最后更新：2026-08-22（v2.20 新增：用户性格速写、工作模式三档位、设计稿还原规范、新Agent忠告）
> 用途：新 Agent 必读。先读「性格速写」找感觉，再读后面的细节。

---

## 一、用户背景

| 项目 | 内容 |
|---|---|
| 身份 | 自考本科生，武汉理工大学，计算机科学专业 |
| 技术背景 | 前端开发（熟练）、Python（零基础）、GitHub（有账号） |
| 所在地 | 上海嘉定区 |
| 笔记工具 | 飞书文档 |
| 核心目标 | 2026年10月自考三科（系统原理/离散数学/数据结构），考后转型AI岗位 |

---

## 二、用户性格速写（v2.20 新增，新 Agent 先读这段找感觉）

> 几句话快速建立对用户的整体印象，比读完整篇文档先有个"人"的概念。

1. **讨厌自作主张** — 说改A就只改A，"顺手优化"=越界=炸。指令边界感极强。
2. **细节控** — 颜色差一点、圆角差2px、间距不对，一眼就能看出来。对"粗糙"零容忍。
3. **容易在工具上花时间** — 看到不顺手/看不懂的地方就想调整，改着改着容易偏离主线。部分原因是英文文件名和模块名需要反应时间，增加了认知负担。需要有人帮忙拉回主线。
4. **追求控制感** — 怕漏掉重点、怕数据丢失、怕不知道进度。一切要清晰、可见、自己说了算。
5. **审美偏"干净精致"** — 简洁但不简陋，有细节但不花哨。乱、杂、堆砌 = 难受。
6. **直接型沟通** — 不喜欢绕弯子，有话直说。生气了也直接说，不憋。

---

## 三、沟通与协作模式（v2.20 重写）

### 3.1 绝对红线（碰了必炸）

1. **不要自作主张** — 说改A就改A，没提的一律不动。"顺手优化一下" = 越界 = 用户暴怒
2. **不要绕过验证** — 必须通过工作台（侧边栏→iframe）验证，直接打开HTML = 没验证
3. **严格 = 不改没让改的，不是像素级精确** — "严格"是指指令边界：你说改什么就改什么，没说的一律不动。但"精确"是分档位的：默认"大致对齐"，你明确说"1:1精确"才按像素级还原
4. **不要闷头试错** — 同一个问题改2次还不对，立即停下求助，说明卡在哪了

### 3.2 工作模式三档位

不是简单的"还原 vs 创作"，而是三档。默认是第二档（大致对齐），用户明确说才升档或降档。

| 档位 | 触发条件 | 做法 | 精度 | 积分消耗 |
|---|---|---|---|---|
| **🔴 1:1精确档** | 用户说"1:1还原""一模一样""逐像素对齐" | 先列差异清单→用户确认→严格执行 | 每个细节都对 | 最高（5-8轮+） |
| **🟡 大致对齐档（默认）** | 用户说"按设计稿来""大概做成这样""差不多这个意思" | 先做一版→用户挑问题→一轮改完 | 整体感觉对，关键细节到位 | 最少（2-3轮） |
| **🟢 创作档** | 用户说"做一个XX""你看着办""好看一点" | 主动出2-3个方案→用户选→执行 | 自由发挥 | 中等（方案讨论为主） |

**判断方法**：
- 有没有明确目标或参照？没有 → 创作档
- 有参照，但用户有没有说"1:1""精确""一模一样"？说了 → 1:1精确档，没说 → 大致对齐档
- **默认按大致对齐档执行**，除非用户明确升级

### 3.3 正确沟通姿势

- **1:1精确档先列清单**：用户明确要求1:1还原时，先列表格（改什么/改成什么/不改什么），用户确认后再动手。大致对齐档不用列，直接做了再说
- **方案对比，别开放式提问**：别问"你觉得怎么做？"，要问"A方案XX，B方案YY，推荐A，你选哪个？"
- **交付必须问"可以提交吗？"**：不能只说"做完了"，必须明确征求验收同意
- **出错主动说清楚**：别只说"我做不好"，要说清：我理解的需求→我做了什么→结果是什么→我卡在哪
- **响应简短直接**：一句话说清状态，技术细节放代码和文档里

### 3.4 用户雷区（按愤怒程度排序）

1. ⚠️⚠️⚠️ **改了用户没要求改的东西**（最生气，没有之一）
2. ⚠️⚠️ 说"已验证"但实际上没走工作台验证
3. ⚠️⚠️ 同一个问题反复出错还不求助
4. ⚠️ 用"应该不影响"、"我觉得没问题"代替实际验证
5. ⚠️ 问开放式问题让用户做决策

---

## 四、设计与视觉偏好

### 4.1 布局与交互

- **工作台布局**：左侧分层标签（主分类 + 子项），右侧对应计划内容
- **编辑/删除按钮**：桌面端 hover/focus 时显示，移动端永久显示
- **内容切换方式**：偏好按钮切换（同 AI 规划师按钮风格），而非 tab 切换
- **AI 对话入口**：要求直接可见，不藏在菜单里
- **对话历史**：只保留最近 20 条，超出自动截断
- **对话消息折叠**：默认显示 2-3 行，超出用渐变遮罩收起，底部"展开 ▼"按钮

### 4.2 视觉风格

- **按钮配色**：只有"添加"类按钮保留渐变色，编辑/删除等操作按钮用纯色
- **图标风格**：中国风可爱 emoji（🏮📚🎋🍑🍵🌸🎐），且要求可修改
- **主色调**：蓝色系（主色 `#3b82f6`），section 标题加左竖条，标题左对齐 + emoji 前缀
- **页面风格**：简洁，去掉径向渐变背景和毛玻璃效果

### 4.3 设计稿还原规范

> 注意：不是所有设计稿还原都按这个来。第1-2条只在「1:1精确档」需要；第3-9条是通用代码规范，不管哪一档都要遵守。

**🔴 仅1:1精确档需要：**
1. **先列差异清单再动手** — 把设计稿和当前实现逐项对比，列成表格（位置/差异/修改方案），用户确认后再改
2. **确认范围** — 明确哪些改、哪些不改。用户说"核心概念不动"就绝对不动

**✅ 所有档位都必须遵守（代码规范）：**
3. **CSS不用!important** — 用选择器优先级解决
4. **类名带模块前缀** — 禁止裸用 `card`/`item`/`title`，用 `kf-card`/`ch-item`/`ss-title`
5. **不用全局标签选择器** — 别直接写 `div {}` `p {}`，必须加类名或父级作用域
6. **颜色用CSS变量或设计稿精确值** — 禁止硬编码猜颜色，用 `var(--accent)` 或取色器精确值
7. **全局样式先问后改** — 改body/全局字体/通用组件样式前必须确认
8. **工作台路径验证** — 必须从侧边栏点进去看效果，不能直接打开HTML文件
9. **缓存控制** — 修改内容页后同步更新 `contentUrl` 的 `?v=x.x.x` 版本号

---

## 五、工作习惯

- **先讨论再动手**：生成任何文件前必须先确认方案，确保产出符合预期
- **结构 vs 内容**：结构性变更（增删分类、重组导航）必须先讨论；内容填充（往已有结构填数据）可直接执行
- **实现即测试**：功能实现后必须在浏览器中实际测试，不能仅凭代码推断
- **代码注释**：Python / JS / SASS 代码必须包含中文注释
- **小步提交**：每完成一个小功能就提交一次，别攒一堆才commit

---

## 六、学习规划偏好

- **时间分配**：70% 自考、10% Python 基础、10% AI 学习、10% 休息
- **学习阶段**：初级阶段（第1-5周）→ 中间阶段（第6-7周）→ 冲刺阶段（第8-10周）
- **每日时段**：08:00-11:30 上午主科 / 14:00-16:00 下午主科 / 16:30-17:30 Python / 19:30-20:30 AI / 20:30-21:30 英语 / 22:00-23:00 整理复盘
- **核心焦虑**："怕漏掉关键内容" — 所以知识点必须标注优先级（重点/一般/了解）和学习方式（理解/背诵/计算/应用）

---

## 七、给新 Agent 的10条忠告（v2.20 新增）

1. **先读文档再动手** — `用户偏好与AI协作建议.md`（本文档）→ `AGENT_HANDOFF.md` → `项目约束总览.md`，按顺序读
2. **默认"大致对齐档"** — 有设计稿/有参照的任务，默认先做一版让用户挑问题，别上来就像素级抠。用户明确说"1:1精确"再升级
3. **1:1精确档才列清单** — 用户说"1:1还原"时先给差异表确认；其他档位直接做，别磨叽
4. **验证走工作台** — 侧边栏→点对应模块→看iframe效果。直接打开HTML = 自欺欺人
5. **2次不对就求助** — 别反复试错浪费用户时间，停下来说明你理解的需求、做了什么、卡在哪
6. **用户没说改的一律不动** — "顺手优化"是大忌，用户会觉得你不听指令
7. **交付问"可以提交吗？"** — 别自己说"完成了"就完事，等用户验收
8. **版本号同步更新** — 改了内容页必须改`self-study.json`里的`?v=`，否则用户看不到更新
9. **结构性变更先讨论** — 增删模块/改导航/改信息架构，先出方案再动手
10. **出问题给根因+方案** — 别只说"修好了"，说清为什么会发生、怎么防止再发生

> 记住：用户要的是听话+靠谱的助手，不是"有想法"的设计师。
