# 工作台优化堆积木

> **规则**：平时想到就加一行，不分类、不排版、写完继续学习。
> **处理时间**：每周日下午统一看，挑 2-3 个最影响体验的改，剩下的继续扔着。
> **判断标准**：不改这个，今天能学习吗？能 → 记在这里；不能 → 立刻改。

---

## 🔴 待验证的 P0 疑似问题

- 题库内容会不会丢失？（之前遇到过，不确定修好了没）
- 掌握程度会不会丢失？（切换浏览器/清缓存后数据还在吗）

---

## 💡 想到什么就加什么

（往下追加，不用管顺序）

- 右边内容区各模块风格不统一，看上去手痒（卡片/标题/按钮/列表样式需要统一规范）
- 🔴 三科知识框架页面 JSON 化：当前章节数据写死在 HTML 里，复制粘贴导致 div 结构不匹配、localStorage 键名串科目、样式改一个忘两个。应改为一个 template.html + 三份 JSON 数据，JS 动态加载。涉及文件：02324/13003/13015 三个知识框架页面 + self-study.json + build.py。预计工作量：中等（提取数据 + 改 JS 加载逻辑 + 验证三科显示正常）
-
