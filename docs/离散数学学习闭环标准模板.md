# 离散数学学习闭环标准模板

> 本文档描述离散数学（02324）前三章已跑通的学习闭环，作为后续章节补充的标准模板。
> 补充新章节时，只需完成「知识框架补全」和「题库补充」两步，三个页面代码无需修改。

---

## 一、闭环全景

```
知识框架JSON → 背诵与简答（记忆+计算卡片）
                    ↓ 引导按钮
              练习测验（三层筛选+答题+AI出题）
                    ↓ 引导按钮
              复盘总结（错题反思+题库管理+章节复盘）
                    ↓ 引导按钮
              回到背诵与简答
```

**五环数据流向**：

1. 知识框架JSON（只读数据源）
2. 背诵与简答（读写掌握度）
3. 练习测验（读写答题记录）
4. 复盘总结（读写题库+错题反思）
5. 页面间引导流（URL参数传递上下文）

---

## 二、第一环：知识框架（数据源）

**文件**：`data/knowledge-framework-02324.json`

### 2.1 每章结构

每章的 `sections` 数组中：

- `sections[0]` 类型为 `核心概念`，包含 `coreConcepts` 数组（或 `items`）
- 可选附加区块：`考核知识点与考核要求`、`常见题型`、`典型例题`、`同步练习`、`易错点`

### 2.2 概念字段规范

每个概念对象必须有：

| 字段 | 必填 | 说明 | 示例 |
|---|---|---|---|
| `term` | 是 | 概念名称 | `命题` |
| `cardType` | 是 | 卡片类型 | `memory` 或 `calculation` |
| `priority` | 是 | 优先级 | `重点` / `一般` / `了解` |
| `level` | 是 | 学习层次 | `背诵` / `理解` / `计算` / `应用` |
| `summary` | 是 | 定义概述 | `能判断真值的陈述句` |
| `points` | 否 | 分条要点 | `["陈述句", "真值唯一确定"]` |
| `ex` | 否 | 举例 | `2是素数（真命题）` |
| `exam` | 否 | 考点 | `判断是否为命题` |
| `hint` | 否 | 学习提示 | `注意真值的唯一性` |

**计算卡额外字段**（cardType 为 calculation 时）：

| 字段 | 必填 | 说明 | 示例 |
|---|---|---|---|
| `formula` | 是 | 公式表达 | `p∨q` |
| `steps` | 是 | 解题步骤数组 | `["识别联结词", "代入变量"]` |
| `answer` | 是 | 标准答案 | `p∨q` |
| `answerAliases` | 否 | 等价答案数组 | `["p ∨ q", "p or q"]` |

### 2.3 当前数据量

- 9章，59个核心概念
- 计算卡12个：第1章8个、第2章1个、第3章2个（部分章节无计算卡）
- 无独立"必会公式"区块，公式散落在核心概念的 calculation 类型卡片中

### 2.4 章节命名规范

- JSON中 `name` 字段格式：`第1章 命题与命题公式`
- 题库中 `chapter` 字段必须与知识框架章节名完全一致
- 学习计划中 `chapter` 使用前缀匹配（如 `第1章` 匹配 `第1章 命题与命题公式`）

---

## 三、第二环：背诵与简答（记忆+计算卡片）

**文件**：`Workbench/自考学习/背诵与简答-核心概念背诵卡.html`

### 3.1 数据读取

`transformKfToCards()` 函数规则：

1. 遍历 `chapters[].sections[]`
2. 只处理 `s.type === '核心概念'` 的区块
3. 读取 `s.coreConcepts || s.items`
4. 以 `c.term` 作为卡片名称和唯一键（`cardKey(c) = c.term || c.question`）
5. 以 `c.cardType` 区分记忆卡/计算卡（默认 memory）
6. 计算卡额外复制 `formula/steps/answer/answerAliases`
7. 合并 `summary + points` 作为 `def` 字段（定义内容）
8. 初始 `mastery` 为 `unknown`

### 3.2 卡片展示

**记忆卡正面**：
- 章节名
- 问题标签（"什么是X？"）
- 提示（如有 `hint`）
- 掌握度标签

**记忆卡背面**：
- 定义（`def` = summary + points）
- 举例（`ex`）
- 考点（`exam`）

**计算卡正面**：
- 章节名 + "· 计算"
- 计算标签
- 问题（`question`）
- 输入框 + 提交按钮
- 符号面板（∧ ∨ ¬ → ↔ ∀ ∃ 等）

**计算卡背面**：
- 定义/公式说明
- 举例
- 考点
- 公式（`formula`）
- 解题步骤（`steps` 数组）
- 最终答案（`answer`）

### 3.3 今日任务匹配

1. 读取 `study-plan.json`
2. 找到 `weeks.find(w => w.isCurrent)`
3. 按科目名称提取当前周次的章节前缀列表
4. 前缀匹配卡片 `chapter` 字段（如 `第1章` 匹配 `第1章 命题与命题公式`）

### 3.4 掌握度记录

- **API**：`POST /api/recite-mastery`，数据结构 `{ [term]: 'known'|'unsure'|'unknown' }`
- **本地备份**：localStorage，key 为 `recite_mastery_02324`
- **恢复顺序**：优先读 API 返回的 `window.__apiMastery`，回退 localStorage

---

## 四、第三环：练习测验（做题练习）

**文件**：`Workbench/自考学习/练习测验.html`，`data/quiz-bank-02324.json`

### 4.1 题库数据结构

**必填字段**：

| 字段 | 说明 | 示例 |
|---|---|---|
| `id` | 唯一标识，格式 `q-02324-XXX` | `q-02324-001` |
| `question` | 题干 | `下列句子中，是命题的是（ ）` |
| `type` | 题型（英文key） | `choice` / `fill` / `calculate` / `shortAnswer` / `essay` / `proof` |
| `chapter` | 章节（与知识框架章节名一致） | `第1章 命题与命题公式` |
| `cardId` | 关联背诵卡term | `命题` |
| `answer` | 答案 | `C` 或 `p∨q` |
| `explanation` | 解析 | 文字 |

**各题型额外字段**：

| 题型 | 额外字段 | 说明 |
|---|---|---|
| `choice` | `options`（数组）、`subType`（single/multi/judge） | 选项文本可带"A. "前缀，渲染时自动去除 |
| `fill` | `blanks`（数组，每项含 `answer` 和 `synonyms`） | 按空格数量评分，60%通过 |
| `calculate` | `formula`、`steps`、`answerAliases` | 归一化后匹配，支持等价答案 |
| `shortAnswer` | `points`（评分点数组）、`referenceAnswer`、`passThreshold` | 按评分点加权匹配 |
| `essay` | 同 shortAnswer | 同上 |
| `proof` | `steps`（含 `keywords`）、`referenceAnswer`、`passThreshold` | 按步骤关键词匹配 |

### 4.2 三层筛选

| 筛选维度 | 配色 | 数据来源 | 选项生成 |
|---|---|---|---|
| 知识点 | 蓝紫色 | 题库 `cardId` 去重 | 每个概念term一个选项 |
| 题型 | 紫色 | 题库 `type` 去重 | 按科目配置的题型列表 |
| 状态 | 青绿色 | 答题记录 `results[q.id]` | 未做/做错/做对 |

**筛选逻辑**：三层条件AND组合

### 4.3 答题记录保存

- **API**：`POST /api/quiz-records`
- **数据结构**：

```json
{
  "subject": "02324",
  "record": {
    "questionId": "q-02324-001",
    "timestamp": "2026-08-25T12:27:49.545Z",
    "type": "choice",
    "chapter": "第1章 命题与命题公式",
    "userAnswer": "C",
    "isCorrect": false,
    "score": 0,
    "total": 1,
    "level": "unknown",
    "session": "today-task"
  }
}
```

### 4.4 题目与背诵卡关联

`cardId` 字段是核心关联键：
- 知识点筛选器从题库 `cardId` 去重生成选项
- 今日任务模式从属于今日章节的题库题目中提取 `cardId`
- 背诵卡背面"练习"按钮跳转到 `cardId` 对应的题目

### 4.5 AI出题

1. 取知识框架前8张卡片
2. 构造prompt（含科目、章节、概念term、要求英文type key）
3. `POST /api/chat` 调用AI
4. `parseAIQuestions()` 解析JSON，去除markdown代码块
5. `TYPE_ALIAS` 标准化中文题型到英文key（如"单选题"→"choice"）

### 4.6 当前数据量

- 135题：第1章69题、第2章33题、第3章33题
- 题型分布：choice 64、fill 33、calculate 16、calc 1、proof 21
- 第4-9章：0题（待补充）

---

## 五、第四环：复盘总结（错题+题库管理+章节复盘）

**文件**：`Workbench/自考学习/复盘总结-章节复盘.html`

### 5.1 题库管理

**加载逻辑**：
1. 并行请求 `/api/quiz-bank?subject=02324` 和 `/api/quiz-records?subject=02324`
2. 构建 `statusMap = { [questionId]: {isCorrect, level} }`（保留最新记录）
3. 按 `chapter` 分组渲染

**三个筛选下拉**：
- 章节下拉（蓝紫色）：按 `q.chapter` 筛选
- 题型下拉（紫色）：按 `q.type` 筛选
- 状态下拉（青绿色）：按 `statusMap[q.id]` 判断
  - 未答：statusMap中无记录
  - 答对：记录存在且 `isCorrect === true`
  - 答错：记录存在且 `isCorrect === false`

**每题卡片头部标签**：⚪未答 / ✓答对 / ✗答错（颜色区分，灰/绿/红）

### 5.2 错题反思

1. 并行加载答题记录和题库
2. 过滤 `isCorrect === false`
3. 按 `questionId` 去重，保留最新记录，统计错误次数
4. 按 `chapter` 分组展示
5. 每题显示：题目、你的答案 vs 正确答案、解析、重做按钮、标记已解决按钮
6. 已解决标记存储在 localStorage（`ss_wrong_solved_02324`）

### 5.3 章节复盘

每章三个文本域：

| 字段 | 图标 | 用途 |
|---|---|---|
| `summary` | 📝 | 本章总结（核心知识点） |
| `wrong` | ❌ | 错题反思（易错点） |
| `plan` | 🎯 | 改进计划（下一步） |

- 存储在 localStorage，key 为 `ss_review_02324`
- 已复盘/待复盘状态自动标记

---

## 六、第五环：页面间引导流

### 6.1 跳转按钮

| 方向 | 按钮文字 | URL参数 |
|---|---|---|
| 背诵卡 → 练习测验 | 📝 练习 | `?subject=02324&from=recite&chapter=第1章...` |
| 练习测验 → 复盘总结 | ✅ 去复盘总结 | `?subject=02324&tab=wrong#wrong` |
| 复盘总结 → 练习测验 | 去练习 | `?subject=02324&from=review` |
| 复盘总结 → 背诵卡 | ✅ 返回背诵卡 | `?subject=02324` |

### 6.2 底部引导流显示逻辑

**练习测验页** `updateGuideFlow()`：
- 无答题记录 → 隐藏
- 有错题 → 显示橙色"去复盘总结"，提示错题数
- 全部正确 → 显示绿色"全部正确，继续保持！"

### 6.3 URL参数清单

| 参数 | 用途 | 传递方向 |
|---|---|---|
| `subject` | 科目代码 | 所有页面间 |
| `from` | 来源标记（recite/review） | 背诵→练习、复盘→练习 |
| `chapter` | 定位章节 | 背诵→练习 |
| `questionId` | 定位题目 | 复盘→练习 |
| `tab` | 定位标签页（wrong/bank） | 练习→复盘 |
| `mode` | 出题模式（weak/today） | 复盘→练习 |

---

## 七、数据读写分类

| 数据 | 位置 | 读写 | 说明 |
|---|---|---|---|
| knowledge-framework JSON | `data/` | 只读 | 知识框架数据源 |
| 背诵卡掌握度 | localStorage + API | 读写 | `saveMastery()` / `restoreMastery()` |
| 学习计划 | `study-plan.json` | 只读 | 周次章节列表 |
| 练习题库 | `/api/quiz-bank` | 读写 | 练习页读、复盘页读写 |
| 答题记录 | `/api/quiz-records` | 读写 | 练习页写、复盘页读 |
| 章节复盘字段 | localStorage | 读写 | 用户手动填写 |
| AI生成题目 | 临时内存 | 临时 | 不写入持久题库 |
| 错题已解决标记 | localStorage | 读写 | 前端持久 |

---

## 八、API 端点清单

| 端点 | 方法 | 用途 | 调用页面 |
|---|---|---|---|
| `/api/recite-mastery` | GET | 读取背诵卡掌握度 | 背诵与简答 |
| `/api/recite-mastery` | POST | 保存背诵卡掌握度 | 背诵与简答 |
| `/api/study-plan` | GET | 读取学习计划 | 背诵与简答、练习测验 |
| `/api/quiz-bank?subject=02324` | GET | 读取题库 | 练习测验、复盘总结 |
| `/api/quiz-bank` | POST | 保存题库（增删改） | 复盘总结 |
| `/api/quiz-records?subject=02324` | GET | 读取答题记录 | 练习测验、复盘总结 |
| `/api/quiz-records` | POST | 追加答题记录 | 练习测验 |
| `/api/chat` | POST | AI生成题目 | 练习测验 |
| `/api/mastery` | POST | 更新章节掌握度 | 练习测验 |

---

## 九、关键文件清单

| 文件 | 路径 |
|---|---|
| 知识框架 | `data/knowledge-framework-02324.json` |
| 题库 | `data/quiz-bank-02324.json` |
| 答题记录 | `data/quiz-records-02324.json` |
| 背诵卡掌握度 | `data/recite-mastery.json` |
| 章节掌握度 | `data/mastery-progress.json` |
| 学习计划 | `data/study-plan.json` |
| 背诵与简答页面 | `Workbench/自考学习/背诵与简答-核心概念背诵卡.html` |
| 练习测验页面 | `Workbench/自考学习/练习测验.html` |
| 复盘总结页面 | `Workbench/自考学习/复盘总结-章节复盘.html` |
| 模块配置 | `data/modules/self-study.json` |

---

## 十、后续章节补充操作指南

补充第4-9章时，只需完成以下两步，三个页面代码无需修改：

### 步骤一：知识框架补全

在 `data/knowledge-framework-02324.json` 对应章节的 `核心概念` 区块中添加概念条目：

- 记忆卡：填 `term`、`cardType: "memory"`、`summary`、`points`、`ex`、`exam`、`hint`
- 计算卡：填 `term`、`cardType: "calculation"`、`summary`、`points`、`formula`、`steps`、`answer`、`answerAliases`、`ex`、`exam`

### 步骤二：题库补充

在 `data/quiz-bank-02324.json` 中添加题目，每题确保：

- `id`：格式 `q-02324-XXX`，全局唯一递增
- `question`：题干
- `type`：英文key（choice/fill/calculate/shortAnswer/essay/proof）
- `chapter`：与知识框架章节名完全一致
- `cardId`：关联概念term（与知识框架中 `term` 一致）
- `answer`：标准答案
- `explanation`：解析
- 按题型补充对应额外字段（见第四章）

### 自动生效

- 背诵卡：`transformKfToCards()` 自动读取新概念生成卡片
- 练习测验：题库新题目自动出现在列表和筛选中
- 复盘总结：题库管理自动显示新题目
- 今日任务：学习计划对应周次激活后自动匹配新章节

### 命名一致性检查

补充时务必确保三处命名一致：
1. 知识框架 `chapters[].name`（如 `第4章 集合`）
2. 题库 `chapter` 字段（必须与上面完全一致）
3. 题库 `cardId` 字段（必须与知识框架 `term` 完全一致）
