# AI 统筹规划师 · 版本 C 完整工作流方案

## 概述

在版本 B（场景按钮 + JSON 解析 + 一键应用）基础上，增加差异对比、历史记录、自动触发和验收反馈闭环，实现全自动化的 AI 学习计划管理。

## 新增功能

### 1. 差异对比视图

调整计划时，左右对比"原计划 vs AI 调整计划"，高亮变化项。

**界面布局**：
```
┌──────────────────┬──────────────────┐
│  📋 原计划        │  🤖 AI 调整方案     │
│  第2周            │  第2周             │
│  ☐ 系统原理 Ch2   │  ☐ 系统原理 Ch2    │  ← 未变（灰色）
│  ☐ 离散数学 Ch2   │  ✏️ 离散数学 Ch2-3  │  ← 修改（黄色高亮）
│  ☐ Python 练习    │  ➕ Python +AI 实践 │  ← 新增（绿色高亮）
│  ☐ 英语 单词150   │  ➖ (已移除)        │  ← 删除（红色删除线）
└──────────────────┴──────────────────┘
```

**变化类型标记**：
- `unchanged`：未变化，灰色显示
- `modified`：内容修改，黄色高亮
- `added`：新增项，绿色高亮
- `removed`：已删除，红色删除线

**应用选项**：
- `[✅ 保留原计划 + 追加调整计划]`：原计划不动，AI 调整作为新条目追加
- `[🔄 完全覆盖]`：用 AI 调整方案替换原计划

### 2. 历史记录

每次 AI 交互保存到 `localStorage['schedule_history']`，可查看时间线。

**数据结构**：
```json
{
  "history": [
    {
      "timestamp": "2026-08-13T22:00:00",
      "action": "generate|adjust|review",
      "phase": 0,
      "weekIndex": 1,
      "summary": "生成第2周计划（5项任务）",
      "changes": {
        "added": 5,
        "modified": 0,
        "removed": 0
      },
      "aiResponse": "AI 原始回复 JSON",
      "applied": true
    }
  ]
}
```

**界面**：
```
┌─────────────────────────────────────┐
│  📜 历史记录                         │
│  ───────────────────────────────    │
│  8/13 22:00  📅 生成第1周计划 (5项)   │
│  8/13 22:15  ✅ 验收今日任务          │
│    → 反馈：3/5完成，建议重点复习 Ch1   │
│  8/14 09:00  🔄 调整第2周计划         │
│    → 变更：修改2项，新增1项            │
│  ───────────────────────────────    │
│  [清空历史]                          │
└─────────────────────────────────────┘
```

### 3. 验收反馈闭环

验收结果保存到对应周的备注中，统筹计划页面显示"AI 验收意见"。

**数据流**：
```
用户在统筹计划勾选完成任务
→ 点击 [✅ 验收今日任务]
→ AI 读取已完成/未完成任务
→ AI 给出反馈（文字 + 建议）
→ 反馈保存到 localStorage['schedule_review_YYYY-MM-DD']
→ 统筹计划对应周卡片底部显示"AI 验收意见"区块
```

**验收意见显示**（在周卡片里程碑下方）：
```
┌─────────────────────────────────────┐
│  🤖 AI 验收意见 (8/14 22:00)         │
│  完成率：3/5 (60%)                   │
│  评价：系统原理第1章完成良好，离散数学   │
│  第1章未完成，建议明天优先补上。        │
│  建议：                              │
│  1. 上午集中精力完成离散数学第1章       │
│  2. Python 练习可以推迟到周末          │
└─────────────────────────────────────┘
```

### 4. 自动触发

当某周所有任务勾选完成时，自动弹出提示。

**触发条件**：
- 某周所有 goals 的 `done` 都为 `true`
- 该周是当前阶段中最后一个完成的周
- 距离上次自动触发超过 1 小时（防重复）

**弹窗内容**：
```
┌─────────────────────────────────────┐
│  🎉 第1周任务全部完成！               │
│  是否让 AI 生成第2周的具体任务？       │
│  [📅 生成下周计划]  [稍后再说]         │
└─────────────────────────────────────┘
```

### 5. 进度看板

AI 面板顶部显示当前阶段的实时进度概览。

**显示内容**：
```
┌─────────────────────────────────────┐
│  当前阶段：初级阶段                   │
│  ████████░░░░░░░  35%  (7/20 项)    │
│  ✅ 已完成：7 项                      │
│  ⏳ 进行中：5 项                      │
│  ⚠️ 落后：3 项（原计划本周应完成）      │
│  📅 距考试：66 天                     │
│  💡 AI 建议：优先攻克离散数学第1章      │
└─────────────────────────────────────┘
```

**数据来源**：
- 完成率：`localStorage['schedule_data']` 中当前阶段的 goals
- 落后项：对比当前日期和周任务的 dates 字段
- 距考试：`localStorage['self-study']` 中的考试日期
- AI 建议：上次验收意见的摘要

## 完整界面布局

```
┌─────────────────────────────────────────────────┐
│  📊 进度看板                                      │
│  当前阶段：初级  ██████░░ 35%  落后3项  距考66天    │
├─────────────────────────────────────────────────┤
│  [📅 生成本周计划] [🔄 调整后续] [✅ 验收今日任务]    │
├──────────────────┬──────────────────────────────┤
│  📋 原计划        │  🤖 AI 调整方案                 │
│  第2周 8/18-8/24  │  第2周 8/18-8/24              │
│  ☐ 系统原理 Ch2   │  ☐ 系统原理 Ch2               │
│  ☐ 离散数学 Ch2   │  ✏️ 离散数学 Ch2-3             │
│  ☐ Python 练习    │  ➕ Python +AI实践             │
│  ☐ 英语 单词150   │  ➖ (已移除)                   │
├──────────────────┴──────────────────────────────┤
│  [✅ 保留原计划+追加调整]  [🔄 完全覆盖]             │
├─────────────────────────────────────────────────┤
│  💬 AI 对话区（补充说明/追问）                       │
│  ┌─────────────────────────────────────────┐    │
│  │ AI: 根据你的进度，我建议第2周重点放在...    │    │
│  │ 用户: 能不能把 Python 任务减一些？         │    │
│  │ AI: 好的，已调整 Python 为 1 道 LeetCode  │    │
│  └─────────────────────────────────────────┘    │
│  [输入消息...]  [发送]                            │
├─────────────────────────────────────────────────┤
│  📜 历史记录                                      │
│  8/13 22:00  📅 生成第1周计划 (5项)                │
│  8/13 22:15  ✅ 验收今日任务 → 3/5完成             │
│  8/14 09:00  🔄 调整第2周计划 → 修改2项+新增1项     │
└─────────────────────────────────────────────────┘
```

## 数据结构总览

| localStorage Key | 用途 | 版本 |
|-----------------|------|------|
| `schedule_data` | 周任务数据（WEEK_DATA） | B+C |
| `schedule_phase_override` | 手动阶段切换 | B+C |
| `schedule_history` | AI 交互历史记录 | C |
| `schedule_review_YYYY-MM-DD` | 每日验收意见 | C |
| `schedule_auto_trigger_ts` | 自动触发防重复时间戳 | C |

## 实现优先级

1. **P0（版本 B 已实现）**：场景按钮 + JSON 解析 + 一键应用
2. **P1（首批增量）**：差异对比 + 验收反馈闭环
3. **P2（后续增量）**：历史记录 + 进度看板
4. **P3（最终增量）**：自动触发

## 预估工作量

| 功能 | 预估时间 |
|------|---------|
| 差异对比视图 | 3-4h |
| 验收反馈闭环 | 2-3h |
| 历史记录 | 2h |
| 进度看板 | 2h |
| 自动触发 | 1-2h |
| **合计** | **10-13h（约 1.5 天）** |

## 注意事项

- 差异对比需要实现 JSON diff 算法（按 tag+tagText 匹配目标项）
- 自动触发弹窗需设置冷却期，避免频繁打扰
- 历史记录数据量增长后需考虑清理策略（建议保留最近 50 条）
- 验收意见存储按日期 key，避免同一日期多次验收覆盖
