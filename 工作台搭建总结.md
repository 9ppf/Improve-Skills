# 此刻便是春天工作台搭建总结

生成时间：2026-08-09  
当前文档版本：v0.3.0（随项目 CHANGELOG.md 同步）

> 本文档按项目版本号迭代更新。每次重大改造后，在「版本更新记录」追加条目，并在「当前状态」「缺陷与优化」中同步最新情况。

## 一、版本更新记录

| 版本 | 时间 | 关键变更 |
|---|---|---|
| v0.1.0 | 2026-08-09 | 完成从静态 HTML 到数据驱动架构的迁移；建立 build.py、模块 JSON、主题系统、校验脚本 |
| v0.2.0 | 2026-08-13 | 统一知识框架页模板；data/ 目录迁移到项目根目录；合并约束文档；修复链接断裂与损坏文件 |
| v0.3.0 | 2026-08-13 | 重构 AI 学习规划师；升级统筹计划面板；5 个科目新增 AI助手入口；build.py 新增 `--open` 参数；构建后浏览器预览约束落地 |

## 二、背景与目标

本项目从一份静态 HTML 页面出发，逐步演进为一个数据驱动、模块可扩展、支持主题切换的个人学习工作台。核心目标有两个：一是把阅读资料等学习内容统一整合到一个入口页面；二是让后续新增模块和维护成本尽可能低，避免每次改内容都直接改动 HTML。

## 三、架构演进

### 第一阶段：静态页面

最初的工作台是一个完整的 HTML 文件，所有模块、数据、样式都写在一起。优点是简单直接，缺点是：

- 新增或修改内容必须编辑 HTML
- 样式容易冲突，尤其是多个模块共用类名
- 无法快速切换主题
- 难以复用和扩展

### 第二阶段：数据驱动架构

现在采用「配置 + 数据 + 转换器 + 构建脚本」的架构：

| 层级 | 作用 | 关键文件 |
|---|---|---|
| 全局配置 | 主题、模块注册表 | `data/workbench.json` |
| 模块数据 | 每个模块的 JSON 数据 | `data/modules/*.json` |
| 转换器 | 把原始内容转为工作台可识别的片段 | `transformers/*.py` |
| 构建脚本 | 读取配置、处理模块、生成最终 HTML | `build.py` |
| 校验脚本 | 检查生成结果的正确性 | `.trae/skills/validate_workbench.py` |
| 最终产物 | 可直接打开的工作台页面 | `Workbench/此刻便是春天.html` |

这种架构下，新增模块只需要准备 JSON 数据和可选转换器，然后运行 `python build.py` 即可，不需要修改 HTML 源码。

## 四、遇到的问题与解决方案

### 1. 模块加载路径错误

**问题**：`workbench.json` 中模块文件路径写成 `modules/xxx.json`，导致 `build.py` 在 `data/modules/modules/` 下寻找文件，找不到。

**解决**：把注册表中的 `file` 字段改为 `xxx.json`，由 `build.py` 自动拼接 `data/modules/` 前缀。

### 2. 阅读模块转换器文件名不匹配

**问题**：验证时发现缺少 `readingHtml` 常量，因为转换器文件命名与模块 id 不一致。

**解决**：把 `transformers/reading.py` 改名为 `transformers/read.py`，与模块 id `read` 对应。

### 3. 主题 token 没有替换

**问题**：模块 JSON 中使用了颜色 token，但生成的 HTML 中还是 token 字符串，没有替换成实际色值。

**解决**：在 `build.py` 中增加 `apply_tokens()` 函数，递归替换 JSON 中的 token 为当前主题对应的色值。

### 4. 静态颜色值未主题化

**问题**：部分 CSS 样式仍写死颜色，切换主题时这些元素颜色不变。

**解决**：增加 `_fix_static_colors()` 函数，把硬编码颜色替换为 CSS 变量。

### 5. 主题切换按钮未出现

**问题**：构建后的 HTML 顶部没有主题切换按钮。

**解决**：完善 `_inject_theme_system()` 的注入逻辑，确保按钮、CSS、JS 都能正确插入，并且幂等（多次构建不会重复注入）。

### 6. dry-run 模式仍触发构建

**问题**：运行 `python build.py --dry-run` 时仍然会执行完整构建并生成备份。

**解决**：重构 `main()` 函数，dry-run 时只扫描并预览清理列表，直接返回，不进入 `build()`。

### 7. 文件说明文档格式不易维护

**问题**：用表格列出所有文件用途，容易出错且不够直观。

**解决**：改用「绿单 / 黄单 / 红单」三色清单，按「能否删除」分组，降低误删风险。

### 8. 空文件夹检测不到

**问题**：根目录下出现两个来源不明的空文件夹，但清理逻辑最初没有识别。

**解决**：在 `build.py` 中增加 `_cleanup_empty_root_dirs()`，遍历根目录下非核心目录，空目录直接删除。

### 9. 临时产物清理后会再生

**问题**：`__pycache__/` 和 `.trae-html-share-packages/` 清理后又会重新出现。

**解决**：把清理规则改为「构建前后各清一次，后清理放在完整校验之后」，并在 `build.py` 文件头注释和说明文档中记录这一规则。

### 10. 知识框架页内部 Tab 与工作台级 Tab 叠加

**问题**：为自考科目知识框架页引入「学习计划 / 知识总览 / 第 N 章」Tab 后，工作台右侧仍显示「计划 / 内容」切换按钮，导致两层 Tab 叠加，界面冗余。

**解决**：
- 在 `data/modules/self-study.json` 的知识框架项上增加 `"renderMode": "content"`，显式声明该页面内部已具备完整导航；
- 在 `templates/workbench.html` 的 `renderTabs()` 中识别 `renderMode === 'content'`，跳过 `plan-tabs` 渲染，直接展示内容 iframe；
- 该字段可复用于任何自带导航的内容页（如学习路线图、独立仪表盘），避免工作台级 Tab 干扰。

经验：内容页若自带交互导航，必须在数据层声明渲染模式，否则会出现「壳中壳」式的重复导航。

## 五、当前状态

目前工作台已经具备以下能力：

### 5.1 基础能力

- 左侧三级标签导航：workspace → category → project
- 阅读模块、自考学习模块、Python 基础、AI 学习、AI 助手角色、能力提升共 6 大模块已接入
- 多主题切换，支持明亮 / 暗色模式
- 构建自动化，一键生成最新工作台 HTML
- `build.py --open` 构建校验通过后自动打开浏览器预览
- 自动清理临时产物和旧备份
- 校验脚本保障生成结果质量
- 文件说明文档采用三色清单，便于识别文件用途
- 内容页支持 `renderMode: "content"`，可在数据层控制是否显示工作台级「计划 / 内容」Tab

### 5.2 学习规划能力

- **统筹计划面板**：能力提升 → 学习驾驶舱，提供考试倒计时、每周时间分配、重点任务清单
- **全局 AI 学习规划师**：AI 学习 → AI 学习规划师，跨科目统筹 Python / AI / 备考科目进度，生成可复制提示词
- **单科 AI 助手**：自考学习 → 任一科目 → AI助手，基于该科目进度生成学习计划、出题、讲错题

### 5.3 自考学习模块

- 4 个科目（13015/02324/13003/00023）已应用统一知识框架页模板
- 知识框架页支持章节级掌握度追踪（4 级状态 + 知识点勾选 + 难度标识 + 重难点摘要 + 顶部进度看板），localStorage 持久化
- 每个备考/未考科目均含：知识框架、真题与错题、背诵与简答、复盘总结、AI助手 5 个子项
- 真题与错题本、背诵卡、复盘总结页面通过 `?subject=XXX` 按科目预选，localStorage 自动保存

### 5.4 AI 与辅助能力

- AI 助手角色统一入口，支持 6 种角色场景
- AI 帮我复盘代码：粘贴代码后自动统计与检查清单，生成提示词
- AI 资讯周报：按分类筛选 + 统计面板 + 周报归档，数据从 `ai-news-data.json` 加载。

## 六、功能缺陷与优化方法

### 6.1 数据内容类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| 真题与错题本、背诵卡目前为空白框架 | 用户无法直接练习和背诵 | 按科目/年份/题型逐步录入真题与核心简答题；可先补充高频考点与近 5 年真题 |
| AI 资讯周报数据为空或滞后 | 资讯页面无内容展示 | 每周六定时任务更新 `Workbench/ai-learning/ai-news-data.json`；或在任务 instruction 中明确「更新数据文件」步骤 |
| 教材例题存在 OCR 识别误差 | 数学符号、字母、图形可能失真 | 建立人工校对流程；图形题优先手动重绘或截图替换；关键公式与符号二次核对 |

### 6.2 交互与体验类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| AI 助手仅生成提示词，不直接调用模型 | 用户仍需手动复制到外部 AI 工具 | 短期保留提示词复制模式；长期可考虑接入大模型 API 或 MCP 实现一键对话 |
| 统筹面板时间分配为手动输入，未与真实日程联动 | 计划与执行脱节 | 与日历/任务系统打通，或增加每日打卡记录，自动计算实际投入时间 |
| 移动端体验未充分验证 | 小屏下侧边栏、Tab、表格可能拥挤 | 在浏览器模拟器中测试 375px / 768px 断点；优化折叠菜单与卡片布局 |
| 页面加载后无全局搜索 | 内容增多后定位困难 | 增加顶部搜索框，支持按模块、科目、标题、关键词筛选 |

### 6.3 工程与校验类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| Node.js 缺失导致 JS 语法校验跳过 | 生成 HTML 中的 JS 错误无法被自动发现 | 安装 Node.js 以启用完整校验；或在 CI/构建脚本中检查 node 可用性 |
| 模块 JSON 数据缺乏版本化管理 | 误操作或批量改造后难以回滚 | 对核心 JSON 数据增加版本字段；重大修改前自动备份；考虑按日期/版本存档 |
| 构建产物 `此刻便是春天.html` 体积较大 | 首次打开较慢 | 评估是否拆分模块懒加载；压缩内联 CSS/JS；仅对开发环境保留可读格式 |
| `--open` 使用系统默认浏览器，无法指定模拟器 | 可能需要手动按 F12 切换设备模拟 | 后续可扩展 `--open-mobile` 参数，直接以指定视口打开浏览器或调用特定浏览器 |

## 七、后续完善建议

按优先级与依赖关系排列：

### 短期（1–2 周）

1. **验证 AI 助手与统筹面板效果**
   - 在浏览器模拟器中依次检查：AI 学习规划师（全局模式）、任一科目 AI助手（单科模式）、学习驾驶舱（统筹面板）。
   - 确认场景按钮、localStorage 进度读取、提示词生成、一键复制等功能正常。

2. **填充真题与背诵内容**
   - 从近 5 年真题与教材课后题中，优先录入高频考点。
   - 按科目分批填充 `真题练习-真题与错题本.html` 与 `背诵与简答-核心概念背诵卡.html`。

3. **修复 OCR 误差**
   - 对 13015/02324/13003/00023 知识框架页中的例题进行人工校对。
   - 图形题手动重绘或替换为清晰截图。

### 中期（1–2 月）

4. **安装 Node.js 启用完整校验**
   - 当前 JS 语法校验被跳过，安装 Node.js 后 `build.py` 与 `validate_workbench.py` 会自动启用 `node --check`。

5. **移动端适配优化**
   - 在 375px / 768px 模拟器中测试侧边栏、Tab、卡片、表格。
   - 优化折叠菜单、字体大小、按钮间距。

6. **主题与搜索扩展**
   - 增加更多主题（如春景、夜读、极简）。
   - 在工作台顶部增加全局搜索，支持按模块、科目、标题、关键词筛选。

### 长期（3 月以上）

7. **数据版本化**
   - 为核心 JSON 数据增加版本字段；重大修改前自动备份；支持按版本回滚。

8. **计划与执行联动**
   - 将统筹面板与日历/任务系统打通，或增加每日打卡，自动计算实际投入时间。

9. **AI 能力升级**
   - 考虑接入大模型 API 或 MCP，让 AI 助手从「生成提示词」升级为「一键对话」。

10. **构建产物优化**
    - 评估模块懒加载、CSS/JS 压缩，降低 `此刻便是春天.html` 首次加载时间。

## 八、核心约定备忘

核心约束已汇总到 `项目约束总览.md`，搭建过程中形成的临时约定和教训见 `project_memory.md`。此处不再重复罗列。
