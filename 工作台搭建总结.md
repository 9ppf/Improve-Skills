# 此刻便是春天工作台搭建总结

生成时间：2026-08-09  
当前文档版本：v2.9.0（随项目 CHANGELOG.md 同步）

> 本文档按项目版本号迭代更新。每次重大改造后，在「版本更新记录」追加条目，并在「当前状态」「缺陷与优化」中同步最新情况。

## 一、版本更新记录

| 版本 | 时间 | 关键变更 |
|---|---|---|
| v0.1.0 | 2026-08-09 | 完成从静态 HTML 到数据驱动架构的迁移；建立 build.py、模块 JSON、主题系统、校验脚本 |
| v0.2.0 | 2026-08-13 | 统一知识框架页模板；data/ 目录迁移到项目根目录；合并约束文档；修复链接断裂与损坏文件 |
| v0.3.0 | 2026-08-13 | 重构 AI 学习规划师；升级统筹计划面板；5 个科目新增 AI助手入口；build.py 新增 `--open` 参数；构建后浏览器预览约束落地 |
| v1.0.0 | 2026-08-13 | 工作台从单一阅读模块扩展为 6 大模块，全部线框占位替换为真实内容页；AI 学习规划师重构、统筹计划面板升级、科目 AI 助手入口、构建后自动预览 |
| v2.0.0 | 2026-08-14 | AI 统筹规划师功能增强（对话折叠/复盘时段/面板调换）；背诵卡页面重构（今日任务/总任务双板块）；背诵卡语法错误修复；约束体系升级至 21 个检查函数 + pre-commit hook |
| v2.1.0 | 2026-08-15 | 背诵卡新增「测验模式」（AI 出题 + 本地降级 + 答题反馈 + 掌握度联动）；dev_server.py 支持手机局域网访问；学习方案科学性优化报告 |
| v2.2.0 | 2026-08-15 | 背诵卡测验模式增强：新增3种题型（计算题/名词解释题/综合应用题，共6种）；出题改为累计+持久化（localStorage）；已答题目测验模式自动隐藏；新增「📚 题库」模式（统计+分类筛选：全部/错题/正确/未答）；取消出题数量限制 |
| v2.3.0 | 2026-08-15 | Python 学习内容重组为2页面+阶段按钮切换：知识点详解（四阶段29个知识点四层内容）、Demo实战（四阶段4个Demo完整设计）、学习闭环（四阶段路线图+五步闭环+阶段筛选）；python.json 页面重命名与描述更新 |
| v2.4.0 | 2026-08-15 | 工作台风格统一（蓝色主题+renderMode 三种渲染模式：bare/content/默认）；Python 模块四项功能优化：手写练习答案折叠、代码一键复制、学习闭环接入真实数据（localStorage `py_kp_progress`）、知识点↔Demo 双向联动（DEMO_MAP + 掌握状态显示） |
| v2.5.0 | 2026-08-16 | AI 学习模块重设计：移除 4 项引用（路线图/自考方案/每日计划/学习规划师）、保留资讯周报、新增 AI 知识图谱（29 知识点×4 阶段）和 AI 实战 Demo（4 个 Demo）；AI 代码复盘迁移至能力提升模块 |
| v2.6.0 | 2026-08-16 | AI 学习新增「岗位突击」分类：岗位技能图谱（25 知识点 17 入门+8 精进，含项目产出和简历写法）+ 学习闭环（考试倒计时+四阶段路线图+五步闭环+3 简历项目追踪） |
| v2.7.0 | 2026-08-17 | 学习驾驶舱按钮样式统一（顶部/阶段/AI场景三组按钮统一尺寸和间距）+ 学习计划时间调整（数据结构提前到Week 2、离散数学9章匀到Week 1-5、AI项目3提前到Week 5启动、阶段日期同步调整） |
| v2.8.0 | 2026-08-17 | 英语（二）模块全面重设计：从3个子项扩展为6个子项（备考指南、词汇系统SM-2算法、题型专项7种、真题模拟计时评分、作文模板5类、AI助手）；解决AI助手501错误（Python内置http.server不支持POST，改用dev_server.py） |
| v2.9.0 | 2026-08-18 | 背诵卡测验题库多项功能增强：AI答疑流式对话+markdown渲染+对话持久化（测验↔题库共享）+复制按钮（header两端对齐）+重做功能+题库跨科目隔离修复 |

## 二、背景与目标

本项目从一份静态 HTML 页面出发，逐步演进为一个数据驱动、模块可扩展、支持主题切换的个人学习工作台。核心目标有两个：一是把阅读资料等学习内容统一整合到一个入口页面；二是让后续新增模块和维护成本尽可能低，避免每次改内容都直接改动 HTML。

## 三、架构演进

### 第一阶段：静态页面

最初的工作台是一个完整的 HTML 文件，所有模块、数据、样式都写在一起。优点是简单直接，缺点是：

- 新增或修改内容必须编辑 HTML
- 样式容易冲突，尤其是多个模块共用类名
- 无法快速切换主题
- 难以复用和扩展

### 第二阶段：数据驱动架构

现在采用「配置 + 数据 + 转换器 + 构建脚本」的架构：

| 层级 | 作用 | 关键文件 |
|---|---|---|
| 全局配置 | 主题、模块注册表 | `data/workbench.json` |
| 模块数据 | 每个模块的 JSON 数据 | `data/modules/*.json` |
| 转换器 | 把原始内容转为工作台可识别的片段 | `transformers/*.py` |
| 构建脚本 | 读取配置、处理模块、生成最终 HTML | `build.py` |
| 校验脚本 | 检查生成结果的正确性 | `.trae/skills/validate_workbench.py` |
| 最终产物 | 可直接打开的工作台页面 | `Workbench/此刻便是春天.html` |

这种架构下，新增模块只需要准备 JSON 数据和可选转换器，然后运行 `python build.py` 即可，不需要修改 HTML 源码。

## 四、遇到的问题与解决方案

### 1. 模块加载路径错误

**问题**：`workbench.json` 中模块文件路径写成 `modules/xxx.json`，导致 `build.py` 在 `data/modules/modules/` 下寻找文件，找不到。

**解决**：把注册表中的 `file` 字段改为 `xxx.json`，由 `build.py` 自动拼接 `data/modules/` 前缀。

### 2. 阅读模块转换器文件名不匹配

**问题**：验证时发现缺少 `readingHtml` 常量，因为转换器文件命名与模块 id 不一致。

**解决**：把 `transformers/reading.py` 改名为 `transformers/read.py`，与模块 id `read` 对应。

### 3. 主题 token 没有替换

**问题**：模块 JSON 中使用了颜色 token，但生成的 HTML 中还是 token 字符串，没有替换成实际色值。

**解决**：在 `build.py` 中增加 `apply_tokens()` 函数，递归替换 JSON 中的 token 为当前主题对应的色值。

### 4. 静态颜色值未主题化

**问题**：部分 CSS 样式仍写死颜色，切换主题时这些元素颜色不变。

**解决**：增加 `_fix_static_colors()` 函数，把硬编码颜色替换为 CSS 变量。

### 5. 主题切换按钮未出现

**问题**：构建后的 HTML 顶部没有主题切换按钮。

**解决**：完善 `_inject_theme_system()` 的注入逻辑，确保按钮、CSS、JS 都能正确插入，并且幂等（多次构建不会重复注入）。

### 6. dry-run 模式仍触发构建

**问题**：运行 `python build.py --dry-run` 时仍然会执行完整构建并生成备份。

**解决**：重构 `main()` 函数，dry-run 时只扫描并预览清理列表，直接返回，不进入 `build()`。

### 7. 文件说明文档格式不易维护

**问题**：用表格列出所有文件用途，容易出错且不够直观。

**解决**：改用「绿单 / 黄单 / 红单」三色清单，按「能否删除」分组，降低误删风险。

### 8. 空文件夹检测不到

**问题**：根目录下出现两个来源不明的空文件夹，但清理逻辑最初没有识别。

**解决**：在 `build.py` 中增加 `_cleanup_empty_root_dirs()`，遍历根目录下非核心目录，空目录直接删除。

### 9. 临时产物清理后会再生

**问题**：`__pycache__/` 和 `.trae-html-share-packages/` 清理后又会重新出现。

**解决**：把清理规则改为「构建前后各清一次，后清理放在完整校验之后」，并在 `build.py` 文件头注释和说明文档中记录这一规则。

### 10. 知识框架页内部 Tab 与工作台级 Tab 叠加

**问题**：为自考科目知识框架页引入「学习计划 / 知识总览 / 第 N 章」Tab 后，工作台右侧仍显示「计划 / 内容」切换按钮，导致两层 Tab 叠加，界面冗余。

**解决**：
- 在 `data/modules/self-study.json` 的知识框架项上增加 `"renderMode": "content"`，显式声明该页面内部已具备完整导航；
- 在 `templates/workbench.html` 的 `renderTabs()` 中识别 `renderMode === 'content'`，跳过 `plan-tabs` 渲染，直接展示内容 iframe；
- 该字段可复用于任何自带导航的内容页（如学习路线图、独立仪表盘），避免工作台级 Tab 干扰。

经验：内容页若自带交互导航，必须在数据层声明渲染模式，否则会出现「壳中壳」式的重复导航。

### 11. JS 字符串引号转义遗漏导致脚本静默崩溃（v2.0.0）

**问题**：背诵卡页面中补元符号 `a'` 的单引号未转义，与 JS 字符串界定符冲突，引发 `SyntaxError` 使整个 `<script>` 块静默崩溃，页面功能全部失效。

**解决**：将所有 JS 内嵌数据中的 ASCII 单引号转义为 `\'` 或替换为中文引号。同时发现 `validate_workbench.py` 从未检查背诵卡页面的 JS 语法，这才是 bug 跨多个版本存在的根本原因。

**经验**：约束文档化 ≠ 约束生效。写了"必须检查引号转义"的约束文档，但没有自动化检查来兜底，等于没有约束。

### 12. AI 调用成功后状态区被清空，用户误以为无响应（v2.1.0）

**问题**：AI 统筹规划师调用成功后，状态提示区被清空，用户看到空白以为请求失败，反复重试。

**解决**：在成功路径显示明确提示文字（如"✅ 计划已生成并应用"），并保留直到用户下次操作。

**经验**：异步操作的 UI 反馈必须在成功和失败两条路径上都有明确提示，不能只在 loading 状态显示。

### 13. AI 生成计划响应时间超过 15 秒（v2.1.0）

**问题**：AI 统筹规划师生成每日计划时响应时间过长，用户频繁取消重试。

**原因**：非流式模式需等待完整响应、生成任务复杂度高（多科目章节进度分析 + 结构化 JSON 输出）、多轮对话上下文累积、未设置 max_tokens 限制。

**解决**：增加进度条动画和阶段性提示文字，让用户感知系统正在工作；限制对话历史为最近 20 条。

### 14. 代码复制功能未实现导致用户看不到效果（v2.4.0）

**问题**：Python 模块页面的"一键复制"按钮点击后无反应，用户反复尝试无效。

**解决**：使用 `navigator.clipboard` API 实现复制功能，并添加 `execCommand` 降级兜底，点击后按钮变绿色"已复制"并在 1.5 秒后恢复。

### 15. 答案折叠功能因 IIFE 作用域问题报错（v2.4.0）

**问题**：Python 知识点页面的手写练习答案折叠功能报 `kpToggleAnswer is not defined` 错误。

**原因**：折叠函数定义在 IIFE（立即执行函数表达式）内部，但 HTML 的 `onclick` 属性在全局作用域中调用，找不到该函数。

**解决**：改用 `addEventListener` 事件监听替代内联 `onclick`，避免作用域问题。

### 16. 浏览器缓存导致看不到代码变更（v2.4.0）

**问题**：修改 HTML/CSS 后刷新页面，仍然显示旧版本。

**解决**：在所有 `contentUrl` 添加 `?v=版本号` 参数，每次更新递增版本号强制浏览器加载新内容。需要 Ctrl+F5 强制刷新作为备用方案。

**经验**：iframe 加载的内容页不会被工作台主页面的刷新同步更新，必须通过 URL 参数控制缓存。

### 17. JS 引号转义问题再次出现（v2.5.0）

**问题**：AI 知识图谱页面中 Python 代码 `print("...")` 的双引号未转义，导致 `SyntaxError: Unexpected identifier`。

**原因**：与 v2.0.0 背诵卡 `a'` 撇号问题同类——内嵌代码到 JS 字符串时，所有 ASCII 双引号必须转义为 `\"`。

**解决**：将代码中的 ASCII 双引号替换为中文引号「」，并用 Python 脚本批量检查其他潜在问题。

**经验**：同一类问题在不同模块重复出现，说明需要将检查规则固化到 `validate_workbench.py` 中，而非依赖人工记忆。

### 18. JS 中 `</script>` 标签未转义（v2.6.0）

**问题**：岗位技能图谱页面中 Vue 代码示例包含 `</script>` 标签，HTML 解析器误认为当前 script 块结束，导致后续代码全部丢失。

**解决**：将 `</script>` 转义为 `<\/script>`。

**经验**：内嵌任何包含 `</script>` 的代码（Vue/React 示例、HTML 模板字符串）都必须转义闭合标签。

### 19. PowerShell 不支持 `&&` 连接命令（v2.6.0）

**问题**：使用 `&&` 连接多个 Git 命令在 PowerShell 中报语法错误。

**解决**：PowerShell 中改用 `;` 分隔命令（`git add .; git commit -m "..."`），或分多次执行。

### 20. contentUrl 版本号未更新导致浏览器缓存旧页面（v2.7.0）

**问题**：学习驾驶舱页面修改了 CSS 和 JS，但用户刷新后看不到任何变化。

**原因**：工作台加载学习驾驶舱的 URL 仍为 `?v=2.5.0`，浏览器直接使用缓存的旧版本页面，完全忽略磁盘上的最新文件。

**解决**：将 `ability.json` 中的 `contentUrl` 版本号从 `v=2.5.0` 更新到 `v=2.6.3`，强制浏览器重新加载。同时在页面内部增加 `SCHEDULE_VERSION` 版本检查机制，自动清除 localStorage 中的旧缓存数据。

**经验**：修改内容页后必须同步更新工作台的 `contentUrl` 版本号，否则用户永远看不到更新。版本号管理应该作为构建流程的自动检查项。

### 21. 页面标题隐藏后残留外边距造成空白（v2.7.0）

**问题**：学习驾驶舱页面顶部的标题（📊 学习驾驶舱）在 v2.4.0 统一风格时被移除，但 `.dash-header-actions` 的 `margin-top: 1rem` 仍然保留，在标题原来的位置留下了一大块空白。

**解决**：将 `margin-top` 从 `1rem` 改为 `0`，并清理所有 `.dash-header` 相关的废弃 CSS 规则。

**经验**：隐藏页面元素时，必须同步清理该元素关联的 CSS 外边距/内边距，否则会留下不可见的"幽灵空白"。这类问题不容易被开发者发现，因为 CSS 外边距不产生报错。

### 22. HTML 中多余的闭合标签（v2.7.0）

**问题**：学习驾驶舱页面 HTML 结构中存在一个多余的 `</div>` 标签，是之前移除 `.dash-header` 包裹层时遗留的。

**解决**：删除多余的 `</div>`，确保 HTML 标签闭合正确。

### 23. Python 内置 http.server 不支持 POST 请求导致 AI 助手 501 错误（v2.8.0）

**问题**：英语模块 AI 助手点击发送后返回 `501 Unsupported method ('POST')` 错误，无法与 DeepSeek API 通信。

**原因**：使用 `python -m http.server` 启动本地预览服务，该内置服务器仅支持 GET/HEAD 请求，不处理 POST。AI 助手的 `/api/chat` 端点需要接收 POST 请求体。

**解决**：改用项目自带的 `dev_server.py` 启动服务，该脚本已实现 `/api/chat` 端点，支持 POST 请求并将请求代理到 DeepSeek API，还支持流式输出（SSE）。

**经验**：Python 内置简易服务器仅适合静态文件预览，涉及 API 交互（POST/PUT/DELETE）时必须使用支持完整 HTTP 方法的自定义服务器。

### 24. 测验题库跨科目混淆——localStorage key 未按科目隔离（v2.9.0）

**问题**：计算机系统原理科目的测验练习中出现了离散数学的题目，两个科目的题库数据互相串扰。

**原因**：题库存储的 localStorage key 为固定值 `recite-quiz-bank`，所有科目共用同一个存储空间，切换科目时未更新 key。

**解决**：将 key 改为 `recite-quiz-bank-{subject}`（如 `recite-quiz-bank-13015`），每个科目独立存储；`switchSubject` 函数同步更新 key 并重新加载题库。AI 对话记录同样改为 `recite-quiz-ai-{subject}` 按科目隔离。

**经验**：任何按科目/用户/会话维度隔离的数据，存储 key 必须包含维度标识符，不能使用固定 key。

### 25. 旧题目无重做按钮——按钮显示逻辑不完整（v2.9.0）

**问题**：题库中新做的题目有「重做」按钮，但之前做的题目没有按钮，无法重复练习。

**原因**：按钮渲染逻辑只在新生成题目时触发，已存在的历史题目未补渲染按钮。

**解决**：改为所有题目均显示按钮——已作答显示「🔄 重做」、未作答显示「✏️ 练习」，通过 `userAnswer !== null` 判断状态决定按钮文字。

**经验**：新增功能时必须考虑历史数据的兼容性，不能只针对新数据生效。

### 26. AI 回答 Markdown 标记未渲染导致可读性差（v2.9.0）

**问题**：AI 答疑回复中的 `**加粗**`、`- 列表`、`` ``代码块`` ``等 Markdown 标记显示为纯文本，用户阅读困难。

**解决**：新增 `renderMarkdown()` 函数，将 AI 回复文本转为 HTML：支持加粗、标题、无序/有序列表、代码块、行内代码、数学公式等格式，并配套 CSS 样式。

**经验**：AI 生成的文本通常包含 Markdown 格式，前端展示时必须经过渲染，否则用户体验极差。

### 27. 复制按钮位置不当——需移至 header 两端对齐（v2.9.0）

**问题**：复制按钮最初放在每条 AI 消息下方，位置分散不易找；用户要求与 AI 答疑标题放在同一元素内两端对齐。

**解决**：将复制按钮从消息级别移至 AI 答疑区域的 header 栏：左侧显示「AI 答疑」标题，右侧显示「复制」按钮和「×」关闭按钮，使用 flex 布局 `justify-content: space-between` 实现两端对齐。点击复制按钮可复制整个对话记录（含【我的问题】【AI 解答】标记）。

**经验**：操作按钮的布局应遵循「聚合原则」——同类操作集中放置在固定区域，而非分散在各条消息中，降低用户查找成本。

## 五、当前状态

目前工作台已经具备以下能力：

### 5.1 基础能力

- 左侧三级标签导航：workspace → category → project
- 阅读模块、自考学习模块、Python 基础、AI 学习、AI 助手角色、能力提升共 6 大模块已接入
- 多主题切换，支持明亮 / 暗色模式
- 构建自动化，一键生成最新工作台 HTML
- `build.py --open` 构建校验通过后自动打开浏览器预览
- 自动清理临时产物和旧备份
- 校验脚本保障生成结果质量
- 文件说明文档采用三色清单，便于识别文件用途
- 内容页支持 `renderMode: "content"`，可在数据层控制是否显示工作台级「计划 / 内容」Tab

### 5.2 学习规划能力

- **统筹计划面板**：能力提升 → 学习驾驶舱，提供考试倒计时、每周时间分配、重点任务清单
- **全局 AI 学习规划师**：AI 学习 → AI 学习规划师，跨科目统筹 Python / AI / 备考科目进度，生成可复制提示词
- **单科 AI 助手**：自考学习 → 任一科目 → AI助手，基于该科目进度生成学习计划、出题、讲错题

### 5.3 自考学习模块

- 4 个科目（13015/02324/13003/00023）已应用统一知识框架页模板
- 知识框架页支持章节级掌握度追踪（4 级状态 + 知识点勾选 + 难度标识 + 重难点摘要 + 顶部进度看板），localStorage 持久化
- 每个备考/未考科目均含：知识框架、真题与错题、背诵与简答、复盘总结、AI助手 5 个子项
- 真题与错题本、背诵卡、复盘总结页面通过 `?subject=XXX` 按科目预选，localStorage 自动保存
- v2.0.0：背诵卡重构为「今日任务/总任务」双板块，按周次自动匹配章节，卡片按掌握度排序；预置 13015（86 张）和 en（6 张）卡片
- v2.1.0：背诵卡新增「测验模式」——AI 根据卡片定义自动生成填空/选择/简答题，答题后自动更新掌握度，AI 不可用时降级为本地出题
- v2.2.0：测验模式增强——新增计算题/名词解释题/综合应用题（共6种题型）；出题改为累计+持久化（localStorage）；已答题目自动隐藏；新增「📚 题库」模式（统计面板+分类筛选：全部/错题/正确/未答）
- v2.9.0：测验题库多项增强——AI答疑流式对话（测验+题库均支持）、markdown渲染、对话持久化（测验↔题库共享，按科目隔离）、复制按钮（header两端对齐）、重做/练习功能（所有题目均可重复作答）

### 5.4 AI 与辅助能力

- AI 助手角色统一入口，支持 6 种角色场景
- AI 帮我复盘代码：粘贴代码后自动统计与检查清单，生成提示词
- AI 资讯周报：按分类筛选 + 统计面板 + 周报归档，数据从 `ai-news-data.json` 加载
- v2.0.0：AI 统筹规划师支持对话式生成每日学习计划（6 时段含复盘），对话消息可折叠，生成计划自动写入课程表
- v2.1.0：dev_server.py 支持 `--host 0.0.0.0`，手机同一 WiFi 下可直接访问工作台
- v2.8.0：英语（二）模块重设计为6子项（备考指南、词汇系统SM-2算法、题型专项7种、真题模拟计时评分、作文模板5类、AI助手），dev_server.py 新增 `/api/chat` 端点代理 DeepSeek API（支持POST和流式SSE）
- v2.9.0：背诵卡测验/题库新增 AI 答疑功能——提交答案后可「🤖 问 AI」展开迷你对话区，AI 自动接收题目+用户答案+参考答案作为上下文流式讲解，支持追问且每题对话独立持久化

## 六、功能缺陷与优化方法

### 6.1 数据内容类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| ~~真题与错题本、背诵卡目前为空白框架~~ | ~~用户无法直接练习和背诵~~ | ✅ v2.0.0 已内置 13015（86 张）和 en（6 张）背诵卡；v2.1.0 新增测验模式。真题与错题本仍待填充 |
| AI 资讯周报数据为空或滞后 | 资讯页面无内容展示 | 每周六定时任务更新 `Workbench/ai-learning/ai-news-data.json`；或在任务 instruction 中明确「更新数据文件」步骤 |
| 教材例题存在 OCR 识别误差 | 数学符号、字母、图形可能失真 | 建立人工校对流程；图形题优先手动重绘或截图替换；关键公式与符号二次核对 |

### 6.2 交互与体验类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| AI 助手仅生成提示词，不直接调用模型 | 用户仍需手动复制到外部 AI 工具 | ✅ v2.0.0 已实现直接对话模式（ai-roles-hub.html 双模式切换）；v2.1.0 测验模式也直接调用 AI |
| 统筹面板时间分配为手动输入，未与真实日程联动 | 计划与执行脱节 | 与日历/任务系统打通，或增加每日打卡记录，自动计算实际投入时间 |
| ~~移动端体验未充分验证~~ | ~~小屏下侧边栏、Tab、表格可能拥挤~~ | ✅ v2.1.0 已支持手机局域网访问（`--host 0.0.0.0`）；响应式适配待实施 |
| 页面加载后无全局搜索 | 内容增多后定位困难 | 增加顶部搜索框，支持按模块、科目、标题、关键词筛选 |

### 6.3 工程与校验类缺陷

| 缺陷 | 影响 | 优化方法 |
|---|---|---|
| ~~Node.js 缺失导致 JS 语法校验跳过~~ | ~~生成 HTML 中的 JS 错误无法被自动发现~~ | ✅ v2.0.0 新增 `check_js_syntax_global()` 全局 JS 语法扫描；Node.js 仍需安装以启用 `node --check`，但全局扫描已覆盖 |
| ~~模块 JSON 数据缺乏版本化管理~~ | ~~误操作或批量改造后难以回滚~~ | ✅ v2.0.0 新增 `check_changelog_coverage()` 自动比对 git 变更与 CHANGELOG；pre-commit hook 自动校验 |
| 构建产物 `此刻便是春天.html` 体积较大 | 首次打开较慢 | 评估是否拆分模块懒加载；压缩内联 CSS/JS；仅对开发环境保留可读格式 |
| `--open` 使用系统默认浏览器，无法指定模拟器 | 可能需要手动按 F12 切换设备模拟 | 后续可扩展 `--open-mobile` 参数，直接以指定视口打开浏览器或调用特定浏览器 |

## 七、后续完善建议

按优先级与依赖关系排列：

### 短期（1–2 周）

1. **落地学习方案优化**
   - `study-plan-optimization/` 中 5 项基础改进（间隔重复、交错学习、时长优化、提前启动数据结构、复盘闭环）尚未实施到工作台。
   - 优先实施间隔重复机制和交错学习调度。

2. **填充真题与背诵内容**
   - 从近 5 年真题与教材课后题中，优先录入高频考点。
   - 按科目分批填充 `真题练习-真题与错题本.html`。
   - 补充离散数学（02324）和数据结构（00023）的预置背诵卡。

3. **修复 OCR 误差**
   - 对 13015/02324/13003/00023 知识框架页中的例题进行人工校对。
   - 图形题手动重绘或替换为清晰截图。

### 中期（1–2 月）

4. **移动端响应式适配**
   - v2.1.0 已支持手机局域网访问，但无响应式布局。
   - 实施渐进式响应式适配：侧边栏改抽屉式、内容页触摸友好、iframe 高度自适应。
   - 可选升级为 PWA（可安装 + 离线）。

5. **AI 出题增强**
   - 简答题评分目前仅靠关键词匹配，可考虑 AI 二次评分。
   - 选择题干扰项质量可提升。
   - 支持按章节/难度/题型筛选出题范围。

6. **主题与搜索扩展**
   - 增加更多主题（如春景、夜读、极简）。
   - 在工作台顶部增加全局搜索，支持按模块、科目、标题、关键词筛选。

### 长期（3 月以上）

7. **数据版本化**
   - 为核心 JSON 数据增加版本字段；重大修改前自动备份；支持按版本回滚。

8. **计划与执行联动**
   - 将统筹面板与日历/任务系统打通，或增加每日打卡，自动计算实际投入时间。

9. **AI 能力升级**
   - 考虑接入大模型 API 或 MCP，让 AI 助手从「生成提示词」升级为「一键对话」。

10. **构建产物优化**
    - 评估模块懒加载、CSS/JS 压缩，降低 `此刻便是春天.html` 首次加载时间。

## 八、核心约定备忘

核心约束已汇总到 `项目约束总览.md`，搭建过程中形成的临时约定和教训见 `project_memory.md`。此处不再重复罗列。
